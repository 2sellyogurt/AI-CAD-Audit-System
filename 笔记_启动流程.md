# 笔记：启动流程分析

## 一、Start.bat 启动流程
```
1. 设置UTF-8编码 (chcp 65001)
2. 查找Python 3.10+ (python/python3/py)
3. 查找路径: %LOCALAPPDATA%\Programs\Python\Python311/312, C:\Python311/312
4. 创建虚拟环境 .venv (如果不存在)
5. 安装依赖 requirements.txt (使用清华源)
6. 复制 .env.template → .env (如果不存在)
7. 设置环境变量:
   - PYTHONUTF8=1
   - PYTHONIOENCODING=utf-8
   - V7_ADMIN_PORT=2708
   - PYTHONPATH=%~dp0src
8. 启动浏览器 http://localhost:2708
9. 执行: .venv\Scripts\python.exe -m v7.admin.server
```

## 二、launcher.py (桌面启动器)
```
功能: 独立EXE打包，双击即用
- 创建Tkinter GUI界面
- 后台线程启动Flask服务器
- 自动打开浏览器
- 端口: 2708 (从.env或配置读取)

关键函数:
- is_server_running(): 检查端口是否被占用
- start_server(): 导入 v7.admin.server.main 并在线程中运行
- open_browser(): 打开 http://localhost:2708/admin/dashboard
```

## 三、调试关键点
| 问题 | 排查方向 |
|------|----------|
| Python找不到 | 检查PATH或安装Python 3.10+ |
| 虚拟环境创建失败 | 检查磁盘空间、权限 |
| 依赖安装失败 | 清华源失败会尝试默认源 |
| 端口被占用 | 修改 .env 中的 V7_ADMIN_PORT |
| 服务启动超时 | 检查 v7.admin.server 导入错误 |

## 四、环境变量清单
```bash
PYTHONUTF8=1
PYTHONIOENCODING=utf-8
V7_ADMIN_PORT=2708
PYTHONPATH=src/
```
