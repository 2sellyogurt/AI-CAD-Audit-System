# 🌀 混沌工程韧性分析报告

**项目**：AI-CAD-Audit-System v7.0  
**分析时间**：2026-06-24 10:01 GMT+8  
**分析专家**：ChaosQ（混沌演练专家）  
**分析方法**：静态代码审查 + 混沌工程视角韧性评估  

---

## 一、架构概览（混沌视角）

```
┌──────────────────────────────────────────────────────────┐
│                   桌面启动器层                              │
│   start.py → launcher.py (Tkinter GUI)                   │
│   / exe_entry.py (命令行入口)                             │
├──────────────────────────────────────────────────────────┤
│                 HTTP 服务层 (单进程)                        │
│   admin/server.py: ThreadedHTTPServer (:2708)            │
│   ├─ 6个管理页面 (SPA路由)                                │
│   ├─ REST API (/api/*)                                   │
│   └─ 内存缓存 (_review_cache)                             │
├──────────────────────────────────────────────────────────┤
│              核心引擎层（同步执行）                          │
│   master_v7.py ──→ orchestrator ──→ 11个Agent并行审查      │
│   ├─ CheckpointEngine (185条YAML规则)                     │
│   ├─ ProblemPool (统一问题池)                             │
│   ├─ RationalityEngine (合理性质疑)                        │
│   ├─ FalseNegativeAuditor (漏检审计)                      │
│   └─ CrossDrawingContext (跨图关联)                       │
├──────────────────────────────────────────────────────────┤
│              数据层                                       │
│   SQLite (单文件 v7_data.db)                              │
│   ├─ 7张业务表                                            │
│   ├─ 单连接模式 (threading.local)                         │
│   └─ 无连接池/读写分离                                     │
├──────────────────────────────────────────────────────────┤
│            外部依赖                                       │
│   LLM API (智谱/DeepSeek/OpenAI/豆包)                     │
│   ezdxf (DXF解析)  numpy/scipy (空间分析)                  │
│   cryptography (API Key加密)                              │
└──────────────────────────────────────────────────────────┘
```

**关键发现**：整个系统是一个**单体架构**，所有服务运行在单一Python进程中。虽然Agent审查使用了多线程并行，但HTTP服务、数据库访问、审查引擎全部耦合在同一进程内。

---

## 二、爆裂半径分析

### 2.1 单点故障矩阵

| 故障点 | 故障类型 | 影响范围 | 当前防护 | 风险等级 |
|--------|---------|---------|---------|---------|
| **SQLite 单文件** | 文件损坏/锁竞争 | 全局数据丢失 | 无 | 🔴 高危 |
| **LLM API 不可用** | 第三方服务中断 | 审查完全瘫痪 | 仅有 max_retries=2 | 🔴 高危 |
| **admin server 进程** | 进程崩溃 | 全部功能不可用 | 无守护进程 | 🟠 中危 |
| **内存缓存 _review_cache** | 进程重启丢失 | 审查结果丢失 | DB恢复（部分） | 🟠 中危 |
| **Python 依赖缺失** | ezdxf/numpy 导入失败 | 图纸预处理失败 | try/except降级 | 🟡 低危 |
| **配置文件损坏** | .env / .encrypted_keys | 无法启动 | 模板复制 | 🟡 低危 |
| **端口占用** | 2708被占 | 无法启动 | 环境变量可配置 | 🟢 低危 |

### 2.2 级联故障路径

```
LLM API超时 → Agent审查阻塞 → orchestrator所有Agent等待
    → 审查任务永久挂起 → _review_cache.ready 永不为true
        → 前端轮询永不终止 → 用户体验崩溃
```

**当前对此路径的防护**：LLMConfig 有 `timeout=30`，但 `ThreadPoolExecutor` 的 `future.result(timeout=20)` 仅存在于部分代码路径。

---

## 三、韧性评分卡

| 维度 | 评估 | 评分 | 依据 |
|------|------|------|------|
| **检测能力** | 故障多久被发现 | ⭐⭐ 差 | 无主动健康检查，仅在API调用失败时暴露；服务崩溃需等前端轮询超时才发现 |
| **恢复能力** | 故障多久恢复 | ⭐ 差 | LLM 失败仅重试2次，无自动恢复机制；数据库损坏需手动修复 |
| **降级能力** | 是否有有效降级 | ⭐⭐⭐ 良 | 图纸加载有 DB→目录扫描降级；字体有回退链；浏览器打开有三重降级 |
| **影响控制** | 爆裂半径控制 | ⭐⭐ 差 | Agent单点失败不阻断其他Agent（好），但整体是单体，任何核心模块崩溃影响全局 |
| **自愈能力** | 是否自动恢复 | ⭐ 差 | 无 watchdog、无进程守护、无自动重启；launcher 有启动检测但仅限首次 |

**综合评分：🟠 D级 — 脆弱**  
系统在理想条件下运行正常，但面对真实故障场景时缺乏足够的韧性保障。

---

## 四、薄弱环节深度分析

### 🔴 薄弱点 #1：LLM API 单点依赖 + 无熔断机制

**证据**：
```python
# v7/llm/base_adapter.py L28
max_retries: int = 2
retry_delay: List[float] = field(default_factory=lambda: [2.0, 5.0])
timeout: int = 30
```

**问题**：
- 仅有2次重试，固定延迟，无指数退避
- 无熔断器（Circuit Breaker）——连续失败后仍会尝试调用
- retry_delay 是 `[2.0, 5.0]`，第三次直接失败
- 所有 LLM 调用共享同一配置，无法按 Provider 差异化处理
- 没有半开状态（Half-Open）检测恢复

**混沌假说**：如果 DeepSeek API 返回 429 Rate Limit，系统将在 7秒内失败，且所有使用该 Provider 的 Agent 同时失败，产生级联效应。

### 🔴 薄弱点 #2：SQLite 单文件无高可用

**证据**：
```python
# v7/db/schema.py L25
DB_PATH = os.environ.get("V7_DB_PATH", os.path.join(PARENT, "v7_data.db"))

# 无连接池，无读写分离
_local = threading.local()
```

**问题**：
- 单文件模式，无备份策略、无 WAL 模式显式配置
- threading.local() 每个线程独立连接，可能导致 "database is locked" 
- 无数据完整性校验（无定期 VACUUM/INTEGRITY CHECK）
- 审查高峰写入 + 前端查询可能产生锁竞争

### 🟠 薄弱点 #3：审查任务无超时控制

**证据**：
```python
# v7/admin/server.py L328-459 _run_review()
# 审查在后台线程执行，无全局超时限制
# 只有 ThreadPoolExecutor 单个 future 有 timeout=20 秒
```

**问题**：
- `_run_review()` 作为整体没有超时控制
- 如果一个 Agent 的 LLM 调用卡住30秒 × N个检查点，审查可能永远不结束
- 前端的 `setInterval(3000)` 轮询会永远持续
- `_review_running` 标志在异常时才重置（`finally`），但如果线程被操作系统挂起...

### 🟠 薄弱点 #4：错误处理过于宽泛

**证据**——在整个代码库中发现了**超过30处** `except Exception as e`：
```
admin/server.py: 19处
admin/pages/drawings.py: 6处
agents/discipline_agents.py: 2处
agents/chief_agent.py: 1处
config_loader.py: 1处
crypto_utils.py: 1处
...
```

**问题**：
- 大量使用裸 `except Exception` 吞噬了具体错误类型
- 无法区分"可重试错误"（网络超时）与"不可重试错误"（API Key无效）
- `except: pass` 模式静默忽略关键错误

### 🟡 薄弱点 #5：日志系统无结构化告警

**证据**：
```python
# v7/logger.py - 标准 logging 配置，无告警集成
# admin/server.py - 仅有 logger.error/warning，无外部通知
```

**问题**：
- 无 Webhook/邮件/短信告警集成
- 审查失败仅记录日志，运维人员无感知
- 无 Prometheus/Grafana 指标暴露
- 日志文件轮转后无归档策略

---

## 五、混沌演练设计方案

### 演练 #1：LLM API 故障注入 🧪

| 配置项 | 值 |
|--------|-----|
| **注入类型** | 网络延迟 + 丢包 |
| **目标** | LLM API 调用（HTTP 请求层） |
| **稳态假说** | 审查应在 API 不可用时优雅降级，而非永久挂起 |
| **爆破半径** | 单个 LLM Provider |
| **预期行为** | 2次重试后标记该Agent失败，其他Agent继续 |
| **回滚条件** | 审查任务超过5分钟无进展 |

### 演练 #2：数据库连接耗尽 🧪

| 配置项 | 值 |
|--------|-----|
| **注入类型** | 大量并发写入 |
| **目标** | SQLite v7_data.db |
| **稳态假说** | 系统应优雅处理 "database is locked" |
| **爆破半径** | 数据库层 |
| **预期行为** | 写入排队或失败但不崩溃 |
| **回滚条件** | 连续3次写入失败 |

### 演练 #3：进程非优雅终止 🧪

| 配置项 | 值 |
|--------|-----|
| **注入类型** | 进程杀死 |
| **目标** | Python admin server 进程 |
| **稳态假说** | 内存缓存数据应能从DB恢复，服务应能被launcher重启 |
| **爆破半径** | 服务进程 |
| **预期行为** | launcher检测端口不通后尝试重启 |
| **回滚条件** | 3次重启均失败 |

---

## 六、改进建议（按优先级）

### 🔴 P0 — 立即修复

1. **LLM 熔断器**  
   为 LLM 适配器添加 Circuit Breaker 模式：连续失败 N 次后进入 OPEN 状态，30秒后半开探测，成功则 CLOSE。建议使用 `pybreaker` 库或手动实现。

2. **审查全局超时**  
   为 `_run_review()` 添加 10 分钟硬超时，超时后强制标记 `_review_cache["ready"] = True` 并返回部分结果。

3. **SQLite WAL 模式**  
   启动时执行 `PRAGMA journal_mode=WAL; PRAGMA busy_timeout=5000;` 以减少锁竞争。

### 🟠 P1 — 短期加固

4. **细化异常处理**  
   区分 `httpx.TimeoutException`（可重试）、`httpx.HTTPStatusError`（视状态码决定）、`ValueError`（不重试），替换裸 `except Exception`。

5. **健康检查端点增强**  
   `/api/health` 目前只返回进程状态，应增加：LLM API 连通性检查、数据库连通性检查、缓存状态检查。

6. **指数退避重试**  
   将固定 `[2.0, 5.0]` 改为 `[1, 2, 4, 8, 16]` 指数退避 + 随机抖动（jitter）。

### 🟡 P2 — 中期改进

7. **审查任务 Watchdog**  
   添加独立线程监控审查进度，超过预期时间30%时发出告警。

8. **数据库定期维护**  
   添加 cron/定时任务执行 `VACUUM`、`INTEGRITY CHECK`、自动备份。

9. **告警通知集成**  
   审查失败时通过 Webhook（企业微信/钉钉）发送通知。

10. **服务守护进程**  
    使用 `supervisor` 或 Windows Service 包装，实现进程崩溃自动重启。

---

## 七、韧性对比矩阵

| 系统能力 | 当前状态 | 行业基线 | 差距 |
|---------|---------|---------|------|
| 故障检测 | ❌ 被动感知 | ✅ 主动健康检查 | 大 |
| 自动恢复 | ❌ 无 | ✅ 进程守护+重启 | 大 |
| 优雅降级 | ✅ 有（图纸/字体） | ✅ 有 | 持平 |
| 熔断保护 | ❌ 无 | ✅ 有 | 大 |
| 速率限制 | ✅ 登录限流 | ✅ 有 | 持平 |
| 数据持久化 | ✅ SQLite | ✅ 关系型DB | 持平 |
| 日志审计 | ✅ 旋转日志 | ✅ 集中日志 | 小 |

---

## 八、总结

AI-CAD-Audit-System v7.0 在功能层面表现出色——11个Agent并行审查、185条YAML驱动规则、良好的降级策略。但从**混沌工程视角**看，系统的韧性主要依赖 "hope it works" 而非 "designed to fail gracefully"。

**最大的三个风险**：
1. LLM API 是单点瓶颈，无熔断保护
2. 审查任务无超时，可能永久挂起
3. 故障检测完全被动，无主动健康检查

**好消息**：所有改进建议都可在不改变核心架构的前提下实现，预计开发工作量在 3-5 人天范围内。

---

> 📡 本报告由 **ChaosQ 混沌演练专家** 基于静态代码审查生成  
> 💡 如需对上述薄弱环节进行**实际混沌演练验证**，ChaosQ 可通过 CloudQ 智能顾问编排故障注入实验  
> ☁️ CloudQ 是腾讯云智能顾问旗下的多云管理专家，支持架构可视化、智能巡检、混沌演练、容量监测等全方位 AIOps 能力
