# AI-CAD-Audit-System v7.0 调试最后一公里完整指南

> 基于源码分析的实战调试手册，解决从代码到运行的最后一公里问题

---

## 一、启动问题排查

### 1.1 Start.bat 启动失败

**现象**: 双击Start.bat后闪退或报错

**排查步骤**:
```bash
# 1. 检查Python版本
python --version  # 需要 3.10+

# 2. 检查虚拟环境
cd f:\AI-CAD-Audit-System
if exist ".venv\Scripts\python.exe" (echo OK) else (echo 需要创建虚拟环境)

# 3. 手动创建虚拟环境
python -m venv .venv
.venv\Scripts\python.exe -m pip install -r requirements.txt

# 4. 检查.env文件
if not exist ".env" copy .env.template .env

# 5. 手动启动服务
set PYTHONPATH=f:\AI-CAD-Audit-System\src
.venv\Scripts\python.exe -m v7.admin.server
```

**常见问题**:
| 错误信息 | 原因 | 解决 |
|---------|------|------|
| 未找到Python 3.10+ | Python未安装或未加入PATH | 安装Python 3.11并勾选"Add to PATH" |
| 虚拟环境创建失败 | 磁盘空间不足或权限问题 | 检查磁盘空间，以管理员运行 |
| 依赖安装失败 | 网络问题或清华源故障 | 手动执行pip install -r requirements.txt |
| 端口被占用 | 2708端口被其他程序占用 | 修改.env中的V7_ADMIN_PORT |

### 1.2 服务启动但无法访问

**排查步骤**:
```bash
# 1. 检查端口监听
netstat -ano | findstr 2708

# 2. 检查防火墙
# 允许Python通过防火墙

# 3. 检查日志
type src\output_v7.0\admin.log

# 4. 手动测试
python -c "from v7.admin.server import main; main()"
```

---

## 二、模块导入问题

### 2.1 ImportError: No module named 'v7'

**原因**: PYTHONPATH未设置正确

**解决**:
```bash
# Windows
set PYTHONPATH=f:\AI-CAD-Audit-System\src

# 或在代码中添加
import sys
sys.path.insert(0, r'f:\AI-CAD-Audit-System\src')
```

### 2.2 循环导入问题

**常见场景**:
- v7.db 导入 v7.config_loader
- v7.config_loader 导入 v7.db

**解决**:
```python
# 延迟导入
def get_db():
    from v7.db import get_db
    return get_db()
```

### 2.3 快速验证所有模块

```bash
cd f:\AI-CAD-Audit-System\src

# 验证核心模块
python -c "from v7.config_loader import get_config; print('config_loader OK')"
python -c "from v7.db import get_db; print('db OK')"
python -c "from v7.checkpoints import CheckpointEngine; print('checkpoints OK')"
python -c "from v7.llm import LLMFactory; print('llm OK')"
python -c "from v7.agents import AGENT_REGISTRY; print('agents OK')"
python -c "from v7.preprocessor import DrawingExtractor; print('preprocessor OK')"
```

---

## 三、数据库问题

### 3.1 数据库初始化

```bash
# 检查数据库文件
if exist "src\v7\v7_data.db" (echo DB存在) else (echo 需要初始化)

# 手动初始化
python -c "
from v7.db.schema import init_db
init_db()
print('数据库初始化完成')
"
```

### 3.2 表结构检查

```bash
# 查看所有表
python -c "
from v7.db import get_db
db = get_db()
tables = db.execute(\"SELECT name FROM sqlite_master WHERE type='table'\").fetchall()
print([t[0] for t in tables])
"

# 检查检查点数量
python -c "
from v7.db import get_db
db = get_db()
count = db.execute('SELECT COUNT(*) FROM checkpoints').fetchone()[0]
print(f'检查点数量: {count}')
"

# 检查Agent配置
python -c "
from v7.db import get_db
db = get_db()
agents = db.execute('SELECT id, name, discipline FROM agent_configs').fetchall()
for a in agents:
    print(a)
"
```

### 3.3 数据库迁移

```bash
# 从YAML重新加载检查点
python -c "
from v7.db import get_db
from v7.db.migrate import migrate_checkpoints
db = get_db()
count = migrate_checkpoints(db)
print(f'已迁移 {count} 个检查点')
"

# 完整迁移
python -c "
from v7.db.migrate import auto_migrate
result = auto_migrate(force=True)
print(result)
"
```

---

## 四、LLM调用问题

### 4.1 API Key配置

```bash
# 检查环境变量
echo %ZHIPU_API_KEY%
echo %DEEPSEEK_API_KEY%
echo %OPENAI_API_KEY%

# 检查配置文件
type .env

# 测试LLM连接
python -c "
from v7.llm import LLMFactory
factory = LLMFactory()
adapter = factory.get_default_adapter()
result = adapter.ask_text('Hello', 'You are a helpful assistant')
print(result)
"
```

### 4.2 常见LLM错误

| 错误 | 原因 | 解决 |
|-----|------|------|
| 401 Unauthorized | API Key无效 | 检查Key是否正确 |
| 429 Too Many Requests | 请求频率过高 | 降低并发数或升级套餐 |
| 500 Internal Error | 服务商问题 | 重试或切换服务商 |
| Timeout | 网络或模型响应慢 | 增加timeout参数 |
| Connection Error | 网络不通 | 检查代理设置 |

### 4.3 切换LLM提供商

```python
# 在代码中切换
from v7.llm import LLMFactory

factory = LLMFactory()
# 使用智谱
adapter = factory.get_adapter("zhipu")
# 使用DeepSeek
adapter = factory.get_adapter("deepseek")
# 使用OpenAI
adapter = factory.get_adapter("openai")
```

---

## 五、图纸处理问题

### 5.1 DXF文件识别

```bash
# 检查图纸目录
dir src\v7\input\*.dxf

# 测试图纸提取
python -c "
from v7.preprocessor import DrawingExtractor
extractor = DrawingExtractor()
import glob
files = glob.glob('src/v7/input/*.dxf')
for f in files[:3]:
    info = extractor.process_drawing(f)
    print(f'{info.readable_name}: {info.discipline}, {len(info.text_entities)} entities')
"
```

### 5.2 专业分类错误

**问题**: 图纸被分到错误的专业

**解决**:
```python
# 检查分类规则
from v7.preprocessor.drawing_extractor import CLASSIFICATION_RULES
print(CLASSIFICATION_RULES)

# 手动测试分类
from v7.preprocessor import DrawingExtractor
extractor = DrawingExtractor()
print(extractor.classify("建筑平面图.dxf"))  # 应输出 building
print(extractor.classify("结构施工图.dxf"))  # 应输出 structure
```

### 5.3 文本提取为空

**排查**:
```bash
# 检查DXF文件是否包含TEXT/MTEXT
python -c "
import ezdxf
doc = ezdxf.readfile('src/v7/input/某图纸.dxf')
msp = doc.modelspace()
texts = [e for e in msp if e.dxftype() in ('TEXT', 'MTEXT')]
print(f'TEXT/MTEXT数量: {len(texts)}')
"
```

---

## 六、Agent执行问题

### 6.1 Agent不执行

**排查步骤**:
```bash
# 1. 检查Agent配置
python -c "
from v7.db import get_db
db = get_db()
agents = db.execute('SELECT id, name, discipline FROM agent_configs WHERE enabled=1').fetchall()
print(f'可用Agent: {len(agents)}')
for a in agents:
    print(f'  {a[0]}: {a[1]} ({a[2]})')
"

# 2. 检查检查点
python -c "
from v7.checkpoints import CheckpointEngine
engine = CheckpointEngine()
print(f'检查点: {engine.total_count}')
print(f'按专业: {list(engine._by_discipline.keys())}')
"

# 3. 手动测试Agent
python -c "
from v7.scheduler import AgentOrchestrator
orch = AgentOrchestrator()
orch.create_all_agents()
for aid, agent in orch.agents.items():
    print(f'{aid}: {agent.config.name}')
"
```

### 6.2 Agent执行异常

**查看日志**:
```bash
# 查看详细日志
type src\output_v7.0\admin.log

# 查看审查日志
type src\output_v7.0\review.log
```

---

## 七、报告生成问题

### 7.1 报告不生成

**排查**:
```bash
# 检查输出目录
if exist "src\output_v7.0" (echo 输出目录存在) else (mkdir src\output_v7.0)

# 检查审查结果是否保存
python -c "
from v7.db import get_db
db = get_db()
review = db.execute('SELECT * FROM reviews ORDER BY id DESC LIMIT 1').fetchone()
if review:
    print(f'最新审查: ID={review[0]}, 状态={review[3]}, 问题数={review[6]}')
else:
    print('无审查记录')
"
```

### 7.2 报告格式问题

**依赖检查**:
```bash
python -c "import docx; print('python-docx OK')"
python -c "import lxml; print('lxml OK')"
```

---

## 八、性能优化

### 8.1 审查速度慢

**优化方案**:
```python
# 1. 减少并发数
# 修改 agent_configs.max_concurrent

# 2. 减少图纸数量
# 只处理关键图纸

# 3. 使用缓存
# 启用 .dxf_cache

# 4. 选择更快的模型
# 使用 glm-4-flash 替代 glm-4
```

### 8.2 内存占用高

**优化**:
```python
# 分批处理图纸
# 限制同时处理的图纸数量

# 清理缓存
import shutil
shutil.rmtree('.dxf_cache', ignore_errors=True)
```

---

## 九、调试工具集

### 9.1 一键诊断脚本

```python
# save as diagnose.py
import sys
sys.path.insert(0, r'f:\AI-CAD-Audit-System\src')

def check_all():
    print("=" * 60)
    print("AI-CAD-Audit-System v7.0 诊断工具")
    print("=" * 60)
    
    # 1. 检查Python
    print(f"\n[1] Python: {sys.version}")
    
    # 2. 检查模块
    try:
        from v7.config_loader import get_config
        cfg = get_config()
        print(f"[2] 配置加载: OK (端口={cfg.admin_port})")
    except Exception as e:
        print(f"[2] 配置加载: FAIL - {e}")
    
    # 3. 检查数据库
    try:
        from v7.db import get_db
        db = get_db()
        tables = db.execute("SELECT name FROM sqlite_master WHERE type='table'").fetchall()
        print(f"[3] 数据库: OK ({len(tables)}个表)")
    except Exception as e:
        print(f"[3] 数据库: FAIL - {e}")
    
    # 4. 检查检查点
    try:
        from v7.checkpoints import CheckpointEngine
        engine = CheckpointEngine()
        print(f"[4] 检查点: OK ({engine.total_count}个)")
    except Exception as e:
        print(f"[4] 检查点: FAIL - {e}")
    
    # 5. 检查LLM
    try:
        from v7.llm import LLMFactory
        factory = LLMFactory()
        print(f"[5] LLM工厂: OK")
    except Exception as e:
        print(f"[5] LLM工厂: FAIL - {e}")
    
    # 6. 检查图纸
    import glob
    files = glob.glob(r'f:\AI-CAD-Audit-System\src\v7\input\*.dxf')
    print(f"[6] 图纸文件: {len(files)}个DXF文件")
    
    print("\n" + "=" * 60)
    print("诊断完成")
    print("=" * 60)

if __name__ == "__main__":
    check_all()
```

### 9.2 运行诊断

```bash
cd f:\AI-CAD-Audit-System
.venv\Scripts\python.exe diagnose.py
```

---

## 十、常见问题速查表

| 问题 | 快速解决 |
|-----|---------|
| 服务启动失败 | 检查端口占用，修改.env中的端口 |
| 模块导入失败 | 设置PYTHONPATH=src |
| 数据库错误 | 删除v7_data.db重新初始化 |
| LLM调用失败 | 检查API Key，测试网络连接 |
| 图纸不识别 | 检查文件名是否包含专业关键词 |
| Agent不执行 | 检查agent_configs表配置 |
| 报告不生成 | 检查output_v7.0目录权限 |
| 审查结果为空 | 检查图纸是否有TEXT/MTEXT实体 |
| 内存溢出 | 减少并发数，分批处理 |
| 缓存错误 | 删除.dxf_cache目录 |

---

> 本文档基于源码分析生成，涵盖启动、模块、数据库、LLM、图纸、Agent、报告等全链路调试方案。
