# AI智能审图系统 v7.0 完整使用指南

> 涵盖安装、使用、维护、卸载、扩展、修改六大维度

---

## 一、安装

### 1.1 环境要求

| 项目 | 要求 |
|------|------|
| 操作系统 | Windows 10/11 (64位) |
| Python | 3.10 或更高版本 |
| 内存 | 建议 8GB+ |
| 磁盘空间 | 建议 2GB+ |
| 网络 | 需要（用于LLM API调用） |

### 1.2 安装方式一：源码安装（推荐开发者）

```bash
# 1. 克隆或解压项目到目录
cd f:\AI-CAD-Audit-System

# 2. 创建虚拟环境
python -m venv .venv

# 3. 激活虚拟环境
.venv\Scripts\activate

# 4. 安装依赖（使用清华镜像加速）
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# 5. 复制环境配置模板
copy .env.template .env

# 6. 编辑 .env 文件，填入API密钥
notepad .env
```

### 1.3 安装方式二：EXE安装包（推荐普通用户）

```
1. 双击运行 AI-CAD-Audit-System-v7.0-Setup.exe
2. 按向导提示完成安装
3. 安装程序会自动：
   - 复制程序文件到 Program Files\AI-CAD-Audit-System
   - 创建桌面快捷方式
   - 创建开始菜单项
   - 写入注册表（用于卸载）
4. 安装完成后自动启动
```

### 1.4 安装方式三：一键启动脚本（最简单）

```bash
# 1. 确保已安装 Python 3.10+
# 2. 双击 Start.bat
# 3. 脚本自动完成：
#    - 检测Python环境
#    - 创建虚拟环境 .venv
#    - 安装依赖（首次约5-10分钟）
#    - 复制 .env.template → .env
#    - 启动服务并打开浏览器
```

### 1.5 依赖清单

```
# 核心依赖
python-docx>=0.8.11    # Word报告生成
ezdxf>=1.2.0           # DXF图纸解析
numpy>=1.24.0          # 数值计算
scipy>=1.10.0          # 科学计算
scikit-learn>=1.2.0    # 机器学习
rtree>=1.1.0           # 空间索引
shapely>=2.0.1         # 几何计算
pyyaml>=6.0            # YAML配置
requests>=2.28.0       # HTTP请求
cryptography>=41.0.0   # 配置加密

# Web UI
flask>=3.0.0           # Web框架

# LLM API
openai>=1.0.0          # OpenAI兼容API

# 测试
pytest>=7.0.0          # 测试框架
```

### 1.6 API密钥配置

编辑 `.env` 文件：

```env
# 智谱AI（推荐，默认）
ZHIPU_API_KEY=your_key_here

# 或 DeepSeek
DEEPSEEK_API_KEY=your_key_here

# 或 OpenAI
OPENAI_API_KEY=your_key_here

# 或 豆包
DOUBAO_API_KEY=your_key_here

# 端口配置（可选）
V7_ADMIN_PORT=2708
V7_REVIEW_PORT=8080
```

---

## 二、使用

### 2.1 启动系统

```bash
# 方式1: 双击 Start.bat
# 方式2: 双击桌面快捷方式
# 方式3: 命令行启动
cd f:\AI-CAD-Audit-System
.venv\Scripts\python.exe -m v7.admin.server
```

访问地址：`http://localhost:2708`

### 2.2 使用流程

```
┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│  1.上传图纸  │ → │  2.启动审查  │ → │  3.查看结果  │
└─────────────┘    └─────────────┘    └─────────────┘
```

**步骤1：上传图纸**
- 将 DXF 文件放入 `src/v7/input/` 目录
- 或在管理后台"图纸管理"页面上传
- 系统自动识别专业分类

**步骤2：启动审查**
- 选择建筑类型（公建/住宅）
- 选择审查深度（快速/标准/深度）
- 点击"开始审查"
- 等待审查完成（时间取决于图纸数量和LLM响应速度）

**步骤3：查看结果**
- "审查结果"页面查看所有问题
- 按严重度(A/B/C/D)筛选
- 按专业筛选
- 按状态筛选
- 导出 Excel/Word 报告

### 2.3 管理后台页面

| 页面 | 功能 | 路径 |
|------|------|------|
| 图纸管理 | 上传/查看/分类图纸 | /admin/drawings |
| 规则与专业维护 | 185条检查点规则管理 | /admin/rules |
| Agent配置 | 11个Agent的启用/模型配置 | /admin/agents |
| API密钥配置 | LLM提供商API密钥管理 | /admin/api-config |
| 审查结果 | 审查问题查看与状态更新 | /admin/results |
| 系统维护 | 缓存清理、日志查看、版本管理 | /admin/system |

### 2.4 快捷键

```
Ctrl+R          刷新当前页面
F5              刷新审查结果
Esc             关闭弹窗
```

---

## 三、维护

### 3.1 日常维护

```bash
# 1. 查看系统状态
# 访问 /admin/system 页面

# 2. 清理缓存
# 访问 /admin/system → 缓存管理 → 清空所有缓存
# 或手动删除 .dxf_cache 目录

# 3. 查看日志
# 访问 /admin/system → 日志查看器
# 或查看文件：
type src\output_v7.0\admin.log
type src\output_v7.0\review.log

# 4. 数据库备份
# 访问 /admin/system → 数据库备份
# 或手动复制 v7_data.db
```

### 3.2 数据库维护

```bash
# 查看数据库统计
python -c "
from v7.db import get_db
db = get_db()
print('项目数:', db.execute('SELECT COUNT(*) FROM projects').fetchone()[0])
print('图纸数:', db.execute('SELECT COUNT(*) FROM drawings').fetchone()[0])
print('检查点数:', db.execute('SELECT COUNT(*) FROM checkpoints').fetchone()[0])
print('审查次数:', db.execute('SELECT COUNT(*) FROM reviews').fetchone()[0])
"

# 重新加载检查点（从YAML）
python -c "
from v7.db import get_db
from v7.db.migrate import migrate_checkpoints
db = get_db()
count = migrate_checkpoints(db)
print(f'已加载 {count} 个检查点')
"

# 数据库迁移
python -c "
from v7.db.migrate import auto_migrate
result = auto_migrate(force=True)
print(result)
"
```

### 3.3 日志管理

```bash
# 日志文件位置
src/output_v7.0/admin.log      # 管理后台日志
src/output_v7.0/review.log     # 审查日志
src/output_v7.0/server.log     # 服务器日志

# 日志轮转配置（自动）
# maxBytes=10MB, backupCount=5
```

### 3.4 性能优化

```bash
# 1. 清理缓存
rmdir /s /q .dxf_cache

# 2. 限制并发数
# 编辑 .env 或修改 agent_configs 表

# 3. 使用更快的模型
# 在 API配置 页面选择 glm-4-flash 替代 glm-4

# 4. 减少图纸数量
# 只上传关键图纸，避免过多无关图纸
```

### 3.5 定期备份

```bash
# 备份数据库
copy src\v7\v7_data.db backups\v7_data_%date:~0,4%%date:~5,2%%date:~8,2%.db

# 备份配置
copy .env backups\.env_%date:~0,4%%date:~5,2%%date:~8,2%

# 备份输出报告
xcopy src\output_v7.0 backups\output_%date:~0,4%%date:~5,2%%date:~8,2% /s /i
```

---

## 四、卸载

### 4.1 源码安装卸载

```bash
# 1. 停止服务
# 关闭命令行窗口或按 Ctrl+C

# 2. 删除项目目录
cd f:\
rmdir /s /q AI-CAD-Audit-System

# 3. 清理环境变量（如有手动添加）
# 删除 PYTHONPATH 中的相关路径
```

### 4.2 EXE安装包卸载

```
方式1: 开始菜单 → AI-CAD-Audit-System → 卸载
方式2: 控制面板 → 程序和功能 → AI-CAD-Audit-System → 卸载
方式3: 运行安装目录下的 uninst.exe
```

卸载程序会自动：
- 删除程序文件
- 删除桌面快捷方式
- 删除开始菜单项
- 清理注册表项

### 4.3 彻底清理

```bash
# 删除残留数据（注意备份重要数据）
# 1. 删除数据库
del %USERPROFILE%\v7_data.db

# 2. 删除缓存
rmdir /s /q %TEMP%\.dxf_cache

# 3. 删除日志
rmdir /s /q %USERPROFILE%\output_v7.0
```

---

## 五、扩展

### 5.1 添加新的检查点规则

**方式1：通过管理后台**
```
1. 访问 /admin/rules
2. 选择专业分类
3. 点击"新建检查点"
4. 填写：
   - ID（唯一标识）
   - 名称
   - 严重度（A/B/C/D）
   - 检查类型
   - 标准代号
   - 标准条款
   - 描述
5. 保存
```

**方式2：通过YAML文件**
```yaml
# src/v7/checkpoints/definitions/my_rule.yaml
checkpoints:
  - id: "building_elevator_count"
    name: "电梯数量检查"
    discipline: "building"
    check_type: "count_min"
    route: "text"
    severity: "B"
    standard_code: "GB50016-2014(2018)"
    standard_clause: "5.5.15"
    description: "检查电梯数量是否满足规范要求"
    enabled: true
    config:
      min_count: 2
      condition: "floor_count > 6"
```

### 5.2 添加新的Agent

```python
# 编辑 src/v7/constants.py
AGENT_CONFIGS = {
    # ... 现有Agent ...
    
    "agent_new_discipline": {
        "name": "新专业工程师Agent",
        "discipline": "new_discipline",
        "role_title": "注册新专业工程师",
        "experience_years": 15,
        "standards": ["GBXXXXX"],
        "system_prompt": (
            "你是新专业工程师，拥有15年设计经验..."
        ),
        "content_keywords": ["关键词1", "关键词2"],
        "primary_disciplines": ["new_discipline", "building"],
    },
}

# 添加专业标签
DISCIPLINE_LABELS["new_discipline"] = "新专业"
```

### 5.3 添加新的LLM提供商

```python
# 编辑 src/v7/constants.py
LLM_PROVIDERS = [
    # ... 现有提供商 ...
    {
        "provider": "new_provider",
        "name": "新提供商",
        "color": "#123456",
        "default_base_url": "https://api.new-provider.com/v1",
        "default_text_model": "model-name",
        "default_vision_model": "vision-model-name",
    },
]

# 添加费用单价
PRICE_PER_MTOK["new_provider"] = {"input": 1.0, "output": 2.0}
```

### 5.4 扩展检查类型

```python
# 编辑 src/v7/checkpoints/checkpoint_schema.py
class CheckType(str, Enum):
    # ... 现有类型 ...
    NEW_CHECK = "new_check"

# 在引擎中实现检查逻辑
# 编辑 src/v7/checkpoints/engine.py
def _execute_new_check(self, checkpoint, context):
    """新检查类型的执行逻辑"""
    # 实现检查逻辑
    return CheckResult(...)
```

### 5.5 自定义报告模板

```python
# 编辑 src/v7/excel_reporter.py 或 generate_three_scene_reports.py
# 修改报告格式、样式、内容
```

---

## 六、修改

### 6.1 修改配置文件

```bash
# 主配置文件
src/v7/config.yaml          # 空间分析配置
src/v7/config/project_config.yaml  # 项目配置
.env                        # 环境变量

# 常用修改：
# 1. 修改端口
V7_ADMIN_PORT=2708

# 2. 修改图纸目录
V7_INPUT_DIR=./input

# 3. 修改输出目录
V7_OUTPUT_DIR=./output_v7.0

# 4. 修改LLM模型
LLM_MODEL=gpt-4o
```

### 6.2 修改检查点规则

```bash
# 方式1: 管理后台直接修改
# 访问 /admin/rules → 编辑

# 方式2: 修改YAML文件
# 编辑 src/v7/checkpoints/definitions/*.yaml

# 方式3: 直接修改数据库
python -c "
from v7.db import get_db
db = get_db()
db.execute('UPDATE checkpoints SET enabled=0 WHERE id=?', ('rule_id',))
db.commit()
"
```

### 6.3 修改Agent配置

```bash
# 方式1: 管理后台
# 访问 /admin/agents → 编辑

# 方式2: 修改 constants.py
# 编辑 src/v7/constants.py 中的 AGENT_CONFIGS

# 方式3: 修改数据库
python -c "
from v7.db import get_db
db = get_db()
db.execute('UPDATE agent_configs SET prompt_text=? WHERE id=?', ('新Prompt', 'agent_id'))
db.commit()
"
```

### 6.4 修改专业分类规则

```python
# 编辑 src/v7/preprocessor/drawing_extractor.py
CLASSIFICATION_RULES = {
    "building": ["建施", "建筑", "总图", ...],
    # 添加新的关键词
    "new_discipline": ["新关键词1", "新关键词2"],
}

# 或编辑 src/v7/constants.py
DISCIPLINE_KEYWORDS = [
    ("新关键词", "new_discipline"),
    # ...
]
```

### 6.5 修改楼层映射

```yaml
# 编辑 src/v7/config.yaml
floor_map:
  新楼层名称: 99
  新楼层英文: 99
```

### 6.6 修改冲突检测参数

```yaml
# 编辑 src/v7/config.yaml
conflict_rules:
  beam_duct:
    overlap_threshold: 300  # 修改阈值
    severity: B             # 修改严重度
```

### 6.7 修改前端样式

```bash
# 管理后台页面样式内联在Python文件中
# 编辑 src/v7/admin/pages/*.py 中的 CSS 部分

# 例如修改颜色主题
# 搜索 "#16213e" 替换为新的主题色
```

### 6.8 修改数据库结构

```python
# 编辑 src/v7/db/schema.py
# 添加新表或字段

# 然后执行迁移
python -c "
from v7.db.schema import init_db
init_db()
"
```

---

## 七、常见问题

### Q: 如何切换LLM提供商？
A: 访问 /admin/api-config → 选择默认提供商

### Q: 如何添加自定义检查规则？
A: 访问 /admin/rules → 新建检查点，或编辑YAML文件

### Q: 如何修改Agent的审查风格？
A: 访问 /admin/agents → 编辑Agent → 修改System Prompt

### Q: 如何导出审查报告？
A: 访问 /admin/results → 点击"导出报告"

### Q: 如何查看审查日志？
A: 访问 /admin/system → 日志查看器

### Q: 如何清理缓存？
A: 访问 /admin/system → 缓存管理 → 清空所有缓存

---

> 版本: v7.0
> 日期: 2026-06-21
> 许可证: MIT
