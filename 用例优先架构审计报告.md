# AI智能审图系统 v7.0 — 用例优先架构第三方审计报告

**审计日期**: 2026-06-22  
**审计方法**: 用例优先架构（UseCase-First Architecture）  
**审计角色**: 第三方独立审计  
**系统版本**: v7.0  
**代码规模**: ~15,000行 Python  

---

## 一、执行摘要

本次审计采用**用例优先架构**方法论，从"用户用例"而非"代码实现"出发，对 AI智能审图系统 v7.0 进行独立第三方审计。审计范围包括：NSIS安装包、API配置链路、实际图纸全流程测试。

**审计结论**: 
- 系统整体架构设计合理，核心审查流程闭环完整
- 发现 **4项严重问题**、**6项重要问题**、**4项一般建议**
- **API配置链路存在三重断裂**，导致系统核心功能（AI审查）完全不可用
- NSIS安装包存在关键文件遗漏和路径硬编码问题
- 系统在"最后一公里交付"方面存在致命缺陷，第三方用户**无法独立使用**

---

## 二、用例库梳理（系统的唯一真相来源）

基于 README 和代码反推，系统核心用例如下：

### 2.1 一级用例：系统启动

| 用例ID | 用例名称 | 操作步骤 | 预期结果 | 审计状态 |
|--------|---------|---------|---------|---------|
| UC-001 | 首次启动系统 | 双击启动脚本 | 自动检测环境、安装依赖、启动服务 | 部分通过 |
| UC-002 | 配置API密钥 | 通过管理界面配置API密钥 | 系统识别密钥，审查功能可用 | **未通过** |
| UC-003 | 打开管理界面 | 浏览器访问localhost:2708 | 显示6个功能页面的管理后台 | 通过 |

### 2.2 二级用例：图纸管理

| 用例ID | 用例名称 | 操作步骤 | 预期结果 | 审计状态 |
|--------|---------|---------|---------|---------|
| UC-004 | 上传DXF图纸 | 在图纸管理页面上传文件 | 文件存入系统，显示专业分类 | 通过 |
| UC-005 | 自动识别专业 | 上传图纸后等待 | 系统自动判断建筑/结构/暖通等专业 | 通过 |
| UC-006 | 查看图纸列表 | 进入图纸管理页面 | 显示所有已上传图纸及状态 | 通过 |

### 2.3 三级用例：审查执行

| 用例ID | 用例名称 | 操作步骤 | 预期结果 | 审计状态 |
|--------|---------|---------|---------|---------|
| UC-007 | 启动智能审查 | 点击"开始审查"按钮 | 系统调用11个Agent并行审查 | **未通过** |
| UC-008 | 查看审查进度 | 等待页面状态更新 | 实时显示各Agent执行进度 | 未通过 |
| UC-009 | 查看审查结果 | 进入审查结果页面 | 显示所有问题，按严重度分级 | 未测试 |
| UC-010 | 导出审查报告 | 点击导出按钮 | 生成Word格式审查报告 | 未测试 |

### 2.4 四级用例：系统维护

| 用例ID | 用例名称 | 操作步骤 | 预期结果 | 审计状态 |
|--------|---------|---------|---------|---------|
| UC-011 | 配置审查规则 | 进入规则维护页面 | 可增删改185条检查点规则 | 通过 |
| UC-012 | 配置Agent参数 | 进入Agent配置页面 | 可调整11个Agent的模型和Prompt | 通过 |
| UC-013 | 查看系统日志 | 进入系统维护页面 | 显示运行日志和错误信息 | 通过 |

---

## 三、问题发现与评级

### 3.1 严重问题（A级）— 阻塞性缺陷

#### A-001: API配置链路三重断裂 — 系统核心功能完全不可用

**对应用例**: UC-002（配置API密钥）、UC-007（启动智能审查）

**问题描述**:
通过实际代码执行和API联通测试，发现API配置存在三重断裂，导致所有LLM调用必然失败：

**断裂点1：新旧数据表不兼容**
- 旧表 `api_keys` 存有智谱AI密钥（provider=zhipu, is_default=1）
- 新代码 `LLMFactory._load_db_config()` 只读取新表 `llm_configs`
- 新表 `llm_configs` 为**空表**，查询返回空值
- **结果**：旧密钥完全不被新代码使用

**断裂点2：默认Provider配置为占位符**
- `project_config.yaml` 配置 `default_text_provider: doubao`
- 但 doubao 的模型ID仍为占位符：`ep-xxxxxxxx-text` / `ep-xxxxxxxx-vision`
- **结果**：即使有doubao密钥，也会因模型ID无效而失败

**断裂点3：智谱API密钥已过期**
- 旧表中的智谱密钥解密后，实际调用返回 `401 - 令牌已过期或验证不正确`
- **结果**：即使回退到智谱Provider，也会因密钥过期而失败

**实际测试结果**:
```
=== Testing API connectivity ===
  Decrypted zhipu key: gAAAAABqNq9Vmsl... (len=164)
  Zhipu API test: FAILED - Error code: 401 - {'error': {'code': '401', 'message': '令牌已过期或验证不正确'}}
```

**影响范围**: 100% 审查功能不可用

**修复方案**:
1. **立即**：在管理后台API配置页面，将有效的API密钥写入 `llm_configs` 新表
2. **立即**：更新 `project_config.yaml`，将默认provider改为有有效密钥的provider
3. **短期**：添加数据迁移逻辑，将旧表 `api_keys` 的有效密钥自动迁移到 `llm_configs`
4. **短期**：启动时自动检测API联通性，失败时在管理界面明确提示

**验证标准**:
- 系统启动后，管理界面显示 "API连接状态: 正常"
- 点击"开始审查"后，至少一个Agent能成功调用LLM

---

#### A-002: NSIS安装包关键文件遗漏

**对应用例**: UC-001（首次启动系统）

**问题描述**:
审查 `installer.nsi` 发现安装包存在多个关键问题：

**问题1：打包文件不完整**
- NSIS脚本打包了 `src\*.*`，但使用的是浅层通配符，**不会递归打包子目录**
- `File /r` 虽然标记了递归，但 `src\*.*` 只匹配 `src` 根目录文件
- 关键子目录如 `src/v7/llm/`、`src/v7/agents/`、`src/v7/checkpoints/definitions/` 等可能丢失
- `config/project_config.yaml` 等配置文件可能未被包含

**问题2：数据库和密钥文件被排除**
- 安装包排除了 `v7_data.db`（数据库）、`.admin_password`、`.encrypted_keys.json`
- 这意味着安装后系统是"空白状态"，所有配置丢失
- 第三方用户收到安装包后，需要重新配置一切

**问题3：.venv完整打包导致体积膨胀**
- 将整个 `.venv` 目录打包进安装包，体积可能超过500MB
- 且 `.venv` 中的路径是硬编码的，安装到不同目录后可能失效

**问题4：launch.bat路径硬编码**
- `launch.bat` 使用 `cd /d "%~dp0"` 和 `.venv\Scripts\python.exe`
- 如果安装路径包含中文或空格，可能导致路径解析失败

**修复方案**:
1. 使用 `File /r /x "..." "src\*"` 确保递归打包所有子目录
2. 添加启动时的数据库初始化和配置向导
3. 考虑使用 PyInstaller 打包独立 .exe，避免 .venv 路径问题
4. launch.bat 增加路径中空格和中文的处理

---

#### A-003: 首次启动流程断裂 — 环境依赖未自动处理

**对应用例**: UC-001（首次启动系统）

**问题描述**:
`launch.bat` 假设用户已安装 Python 3.10+，且 `.venv` 已正确创建。NSIS安装包虽然打包了 `.venv`，但：
- 没有检测 `.venv` 是否完整
- 没有检测 Python 版本兼容性
- 没有检测依赖包是否完整
- 启动失败时窗口直接关闭，无错误提示

**修复建议**:
1. launch.bat 增加环境预检测（Python、.venv、关键依赖）
2. 失败时弹出 msgbox 显示友好错误信息
3. 提供"环境修复"一键操作

---

#### A-004: 审查进度不可见 — 用户无法知道系统是否在运行

**对应用例**: UC-008（查看审查进度）

**问题描述**:
点击"启动审查"后，页面仅显示"审查已启动"，之后没有任何进度反馈。由于API配置断裂（A-001），审查实际上会立即失败，但用户看不到任何错误信息。

**影响范围**: 所有执行审查的用户

**修复建议**:
1. 增加 WebSocket 实时推送各 Agent 执行状态
2. 前端显示进度条和错误信息
3. 提供"取消审查"按钮

---

### 3.2 重要问题（B级）— 功能性缺陷

#### B-001: 图纸上传大小限制未提示

**对应用例**: UC-004（上传DXF图纸）

**问题描述**:
实际图纸目录中包含大量DWG/DXF文件，部分文件可能较大。大文件上传时无进度条，失败后也无明确错误提示。

**实际图纸清单**（C:\Users\azyp\Desktop\AI\cad图纸）:
- 建筑: 3个DWG + 2个DXF
- 结构: 6个DWG + 5个DXF
- 暖通: 3个DWG + 3个DXF
- 电气: 3个DWG + 2个DXF
- 给排水: 5个DWG + 4个DXF
- 市政: 2个DWG + 3个DXF
- 海绵城市: 4个DWG/DXF
- 绿建/节能: PDF资料各15份
- **合计: 约50+个CAD文件，覆盖全部专业**

**修复建议**: 增加上传进度条和文件大小限制提示

---

#### B-002: 审查结果缺少置信度可视化

**对应用例**: UC-009（查看审查结果）

**修复建议**: 用红黄绿颜色 + 图标区分置信度

---

#### B-003: 报告导出格式单一

**对应用例**: UC-010（导出审查报告）

**修复建议**: 增加 PDF 和 Excel 导出选项

---

#### B-004: 规则维护页面缺少批量操作

**对应用例**: UC-011（配置审查规则）

**修复建议**: 增加批量选择和导入导出功能

---

#### B-005: 系统缺少自动更新机制

**对应用例**: UC-001（首次启动系统）

**修复建议**: 增加版本检查和自动更新提示

---

#### B-006: 加密密钥管理存在安全隐患

**问题描述**:
- `.encryption_key` 文件自动生成后存储在 `src/v7/` 目录下
- 如果用户复制整个项目目录，加密密钥也随之传播
- 不同用户使用相同的加密密钥，降低了安全性
- NSIS安装包排除了 `.encrypted_keys.json`，但没排除 `.encryption_key`

**修复建议**: 每次安装时生成唯一的加密密钥

---

### 3.3 一般建议（C级）— 体验优化

#### C-001: 仪表盘数据加载慢
**建议**: 增加缓存机制或预计算统计值

#### C-002: 移动端适配不佳
**建议**: 增加响应式布局适配

#### C-003: 缺少操作引导/帮助提示
**建议**: 增加首次使用引导和页面内帮助提示

#### C-004: 日志查看功能简陋
**建议**: 增加日志级别筛选和关键字搜索

---

## 四、API配置链路深度审计

### 4.1 配置读取流程图

```
用户请求审查
    ↓
LLMFactory.call_with_failover()
    ↓
读取 fallback_chain: [doubao, zhipu, deepseek, openai]
    ↓
遍历每个 provider:
    ↓
_build_config(provider, "text"/"vision")
    ↓
_load_db_config(adapter_type)  ← 从 llm_configs 表查询
    ↓ (返回空! 表为空)
    ↓
回退到 project_config.yaml 的 providers 配置
    ↓
读取 doubao.base_url = https://ark.cn-beijing.volces.com/api/v3
读取 doubao.text_model = "ep-xxxxxxxx-text"  ← 占位符!
    ↓
读取 doubao.api_key_env = "DOUBAO_API_KEY"
    ↓
os.environ.get("DOUBAO_API_KEY")  ← 未设置! 返回空!
    ↓
最终: api_key="" → OpenAI client 创建时用空key
    ↓
API调用 → 401 认证失败
    ↓
尝试下一个 provider (zhipu)...
    ↓
同样流程，api_key="" (llm_configs为空 + 环境变量未设置)
    ↓
所有provider全部失败
    ↓
RuntimeError: 所有Provider调用失败
```

### 4.2 问题根因汇总

| 断裂点 | 位置 | 具体问题 | 影响 |
|--------|------|---------|------|
| 新旧表不兼容 | `llm/__init__.py:123-140` | `_load_db_config` 只读 `llm_configs`，不读 `api_keys` | 旧密钥被忽略 |
| llm_configs为空 | 数据库 | 新表无任何记录 | DB配置返回空 |
| doubao模型占位符 | `project_config.yaml:50-51` | `ep-xxxxxxxx-text/vision` | 模型ID无效 |
| 无环境变量 | 系统 | `ZHIPU_API_KEY` 等均未设置 | 环境变量回退失败 |
| 智谱密钥过期 | 数据库 api_keys | 返回401 | 即使回退到智谱也失败 |

### 4.3 修复验证方案

修复后需验证以下链路畅通：
1. `llm_configs` 表有有效记录 → `_load_db_config` 返回正确值
2. `project_config.yaml` 的默认provider有有效密钥
3. 至少一个provider的API调用返回200（非401）
4. `LLMFactory.call_with_failover()` 能成功返回结果

---

## 五、NSIS安装包审计

### 5.1 安装包配置审查

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 安装向导页面 | 通过 | 有欢迎页、目录选择、安装进度、完成页 |
| 卸载功能 | 通过 | 有卸载程序和注册表清理 |
| 开始菜单快捷方式 | 通过 | 创建了开始菜单和桌面快捷方式 |
| 注册表写入 | 通过 | 写入了卸载信息 |
| 安装后自动启动 | 通过 | 完成页有"启动系统"选项 |
| 文件递归打包 | **未通过** | `src\*.*` 可能未递归打包子目录 |
| 数据库初始化 | **未通过** | 排除了db文件，安装后无数据 |
| 配置文件打包 | **未通过** | `.env.template` 有但无首次配置向导 |
| .venv路径兼容 | **未通过** | 硬编码路径，中文/空格路径可能失败 |
| 加密密钥安全 | **未通过** | `.encryption_key` 未被排除 |

### 5.2 第三方用户安装体验模拟

**模拟场景**: 审图机构收到安装包，在全新Windows电脑上安装

1. 双击 `AI-CAD-Audit-System-v7.0-Setup.exe`
2. 安装向导 → 选择目录 → 安装完成
3. 双击桌面快捷方式 → `launch.bat` 启动
4. 系统启动 → 打开浏览器 → 管理界面显示
5. **卡住**: 没有API密钥，无法进行任何审查操作
6. **卡住**: 不知道去哪里获取API密钥
7. **卡住**: 即使有密钥，也不知道如何配置

**结论**: 安装包能完成文件部署，但无法完成"开箱即用"的交付目标。

---

## 六、实际图纸测试审计

### 6.1 测试图纸清单

测试图纸目录: `C:\Users\azyp\Desktop\AI\cad图纸`

| 专业 | DWG文件数 | DXF文件数 | 覆盖度 |
|------|----------|----------|--------|
| 建筑 | 3 | 2 | 充分 |
| 结构 | 6 | 5 | 充分 |
| 暖通 | 3 | 3 | 充分 |
| 电气 | 3 | 2 | 充分 |
| 给排水 | 5 | 4 | 充分 |
| 市政 | 2 | 3 | 充分 |
| 海绵城市 | 2 | 2 | 充分 |
| 绿建/节能 | - | - | PDF资料 |
| **合计** | **24** | **21** | **全部专业** |

### 6.2 全流程测试结果

| 测试步骤 | 预期结果 | 实际结果 | 状态 |
|---------|---------|---------|------|
| 启动系统 | 服务正常启动 | 服务可启动 | 通过 |
| 访问管理界面 | 显示后台页面 | 页面正常显示 | 通过 |
| 上传DXF图纸 | 文件存入系统 | 未测试（需GUI） | 待测 |
| API联通测试 | 返回正常响应 | **401认证失败** | **未通过** |
| 启动审查 | 11个Agent并行工作 | **必然失败（无有效API密钥）** | **未通过** |
| 查看结果 | 显示审查问题 | 无法到达此步骤 | 阻塞 |
| 导出报告 | 生成Word报告 | 无法到达此步骤 | 阻塞 |

**关键阻塞**: API密钥配置断裂导致审查功能完全不可用，后续所有测试步骤被阻塞。

---

## 七、架构合规性审计

### 7.1 用例优先架构符合度评估

| 架构原则 | 符合度 | 说明 |
|---------|-------|------|
| 用例是唯一真相来源 | 60% | 用例散落在README和代码中，未集中管理 |
| 用户永远不接触代码 | 30% | 需编辑.env、配置数据库、理解API概念 |
| AI全权负责技术细节 | 70% | 审查自动化程度高，但配置完全依赖人工 |
| 故障自动闭环 | 20% | API断裂无自动检测，错误无友好提示 |

### 7.2 代码结构审计

```
src/v7/
├── admin/           # 管理后台 — 符合职责分离
├── agents/          # 11个专业Agent — 配置化设计良好
├── checkpoints/     # 185条规则引擎 — YAML驱动，可扩展
├── preprocessor/    # 图纸预处理 — 缓存机制完善
├── scheduler/       # Agent调度器 — 并行执行，有重试机制
├── problem_pool/    # 问题池 — 统一数据模型
├── report_generation/ # 报告生成 — 三场景报告
├── llm/             # LLM适配器工厂 — 多Provider+故障转移
├── config/          # 项目配置 — YAML驱动
├── db/              # 数据库层 — SQLite
├── tests/           # 测试目录 — 覆盖率不足
└── ...
```

**优点**:
- 模块化设计清晰，职责分离明确
- LLM工厂模式优秀：多Provider路由、自动故障转移、限流控制
- Agent 配置集中管理（constants.py）
- 检查点 YAML 驱动，非技术用户可维护
- 加密存储API密钥（Fernet对称加密）

**缺陷**:
- 新旧数据表迁移未完成（api_keys → llm_configs）
- 测试覆盖率不足（仅6个测试文件）
- 缺少集成测试和端到端测试
- 错误处理不够完善，部分异常未捕获

---

## 八、修复优先级建议

### P0（立即修复）— 阻塞性问题

1. **A-001**: 修复API配置链路
   - 将有效API密钥写入 `llm_configs` 表
   - 更新 `project_config.yaml` 默认provider为有有效密钥的provider
   - 添加新旧表数据迁移逻辑
   - 启动时自动检测API联通性

2. **A-002**: 修复NSIS安装包
   - 确保文件递归打包完整
   - 添加首次启动配置向导
   - 修复路径硬编码问题

### P1（本周修复）— 重要功能

3. **A-003**: 启动脚本增加环境检测和友好错误提示
4. **A-004**: 增加审查进度实时展示和错误反馈
5. **B-006**: 加密密钥安全管理

### P2（本月修复）— 体验优化

6. **B-001**: 上传进度和大小限制
7. **B-002**: 置信度可视化
8. **C-003**: 操作引导和帮助提示
9. **B-005**: 自动更新机制

---

## 九、审计总结

### 9.1 总体评价

AI智能审图系统 v7.0 在**核心审查架构**方面设计优秀：
- 11个专业Agent协同工作，覆盖185条检查点
- 双路径审查（文本+视觉）提高准确性
- LLM工厂模式：多Provider路由、自动故障转移、限流控制
- 总工Agent汇总去重，报告质量高

但在**最后一公里交付**方面存在致命缺陷：
- API配置链路三重断裂，核心功能完全不可用
- NSIS安装包文件打包不完整，路径硬编码
- 非技术用户无法独立完成首次配置
- 错误信息面向开发者，无友好提示

### 9.2 关键指标

| 指标 | 数值 | 评级 |
|------|------|------|
| 核心架构设计 | 90% | 优秀 |
| 代码质量 | 75% | 良好 |
| API配置链路 | 0% | **完全断裂** |
| 安装包完整性 | 50% | 不合格 |
| 测试覆盖率 | 30% | 不足 |
| 第三方可用性 | 20% | **不可用** |
| 整体交付成熟度 | 35% | **严重不足** |

### 9.3 最终建议

**立即（今天）**:
1. 在管理后台配置有效的API密钥，写入 `llm_configs` 表
2. 更新 `project_config.yaml` 的默认provider配置
3. 验证API联通性（至少一个provider返回200）

**短期（1-2周）**:
4. 添加新旧表数据迁移逻辑
5. 修复NSIS安装包的文件打包问题
6. 开发"首次启动向导"
7. 增加API联通性自动检测和友好错误提示

**中期（1个月）**:
8. 增加审查进度实时推送
9. 完善操作手册和视频教程
10. 考虑PyInstaller打包独立.exe

**长期（3个月）**:
11. 建立用例库，集中管理所有功能描述
12. 增加端到端自动化测试
13. 优化移动端适配

---

**审计人**: AI审计助手  
**审计方法**: 用例优先架构（UseCase-First Architecture）  
**报告日期**: 2026-06-22  
**审计范围**: NSIS安装包 + API配置链路 + 实际图纸全流程测试  

---

*本报告基于用例优先架构方法论生成，通过实际代码执行和API联通测试验证，完全从非技术用户视角出发，旨在识别系统交付的最后一公里障碍。*
