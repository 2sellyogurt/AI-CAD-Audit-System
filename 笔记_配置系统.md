# 笔记：配置系统分析

## 一、config_loader.py (统一配置加载器)
```
功能:
- 自动加载 .env 文件 (轻量级实现，不依赖python-dotenv)
- 提供类型安全的Config类
- 查找项目根目录 (向上找包含 v7/ 的目录)

配置优先级:
1. 环境变量 (已存在的不覆盖)
2. .env 文件
3. .env.template (兜底)

关键属性:
- admin_port: V7_ADMIN_PORT (默认2708)
- review_port: V7_REVIEW_PORT (默认8080)
- input_dir: V7_INPUT_DIR
- output_dir: V7_OUTPUT_DIR
- llm_api_key: LLM_API_KEY
- llm_base_url: LLM_BASE_URL
- llm_model: LLM_MODEL
```

## 二、config.yaml (空间分析配置)
```
职责: 楼层映射、专业分类、图层规则、冲突检测参数
(注意: LLM配置在 config/project_config.yaml)

楼层映射 (floor_map):
- 支持中文和英文楼层命名
- 地下: B1F, B2F, B3F, 地下一层... → -1, -2, -3
- 地上: 1F, 2F... 一层, 二层... → 1, 2...
- 特殊: 设备层→50, 夹层→0.5, 屋顶→13

专业分类规则 (discipline_rules):
- hvac: h-, nt-, 暖通, hvac, air, duct
- structure: s-, 结施, 结构, 基础, 配筋
- plumbing: 水施, 给排水, 给水, 排水
- electrical: 电施, 电气, 配电, 照明

图层规则 (layer_rules):
- duct: 风管、送风、排风、排烟...
- pipe: 管、给水、排水、消防...
- beam: 梁 (排除管、风管、标注)
- column: 柱
- wall: 墙

冲突检测规则 (conflict_rules):
- beam_duct: 梁与风管重叠 (阈值300mm)
- beam_pipe: 梁与管线重叠 (阈值200mm)
- column_pipe: 柱与管线冲突 (阈值100mm)
- pipe_crossing: 管线交叉 (阈值200mm)
- duct_wall: 风管穿墙 (阈值200mm)
- egress_width: 疏散宽度 (1100-4400mm)

运行时参数 (runtime):
- max_file_size_mb: 150
- spatial_tolerance: 500
- cache_dir: .dxf_cache
```

## 三、调试关键点
| 问题 | 排查方向 |
|------|----------|
| 配置不生效 | 检查 .env 文件是否存在、格式是否正确 |
| 端口冲突 | 修改 .env 中的 V7_ADMIN_PORT |
| 楼层识别错误 | 检查 floor_map 是否包含该楼层命名 |
| 专业分类错误 | 检查 discipline_rules 前缀匹配 |
| 冲突检测漏报 | 检查 overlap_threshold 是否过大 |
| 缓存问题 | 检查 .dxf_cache 目录权限 |

## 四、.env.template 示例
```bash
V7_ADMIN_PORT=2708
V7_REVIEW_PORT=8080
V7_INPUT_DIR=./input
V7_OUTPUT_DIR=./output_v7.0
LLM_API_KEY=your-api-key
LLM_BASE_URL=https://api.openai.com/v1
LLM_MODEL=gpt-4
```
