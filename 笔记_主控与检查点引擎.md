# 笔记：主控模块与检查点引擎分析

## 一、master_v7.py (主控入口)
```
功能: 一键串联全流程

运行模式:
├── 默认模式: 完整流程 (需DXF+LLM API Key)
├── --dry-run: 干跑模式 (无LLM调用，仅验证流程)
├── --review-only: 仅启动人工复核界面
└── --demo: 演示模式 (模拟数据+启动复核界面)

环境变量:
├── ZHIPU_API_KEY: 智谱API Key (文本+视觉默认)
├── DEEPSEEK_API_KEY: DeepSeek API Key (备用)
└── OPENAI_API_KEY: OpenAI API Key (复杂视觉检查)

干跑测试流程 (run_dry_run):
[1/6] 加载检查点引擎
    → CheckpointEngine() → 输出检查点数量
[2/6] 图纸预处理
    → DrawingExtractor() → 处理DXF文件
    → StandardizationChecker() → 标准化校验
[3/6] 创建Agent集群
    → AgentOrchestrator() → create_all_agents()
    → 输出每个Agent的人格Prompt长度
[4/6] 扫描图纸
    → DrawingScanner() → 确定相关专业
[5/6] 执行审查
    → 各专业Agent.execute() → ProblemPool收集问题
[6/6] 输出报告
    → 生成报告文件

完整流程依赖模块:
├── v7.llm.LLMFactory
├── v7.checkpoints.CheckpointEngine
├── v7.preprocessor.DrawingExtractor, StandardizationChecker
├── v7.problem_pool.ProblemPool, UnifiedIssue, Provenance, DataSource
├── v7.agents.AGENT_REGISTRY, ChiefAgent
├── v7.scheduler.AgentOrchestrator
├── v7.audit.FalseNegativeAuditor
├── v7.scanner.DrawingScanner
├── v7.cross_drawing.CrossDrawingContext
├── v7.conflict_diff
└── v7.conflict_state_manager
```

## 二、CheckpointEngine (检查点引擎)
```
职责: YAML驱动检查点管理

核心能力:
├── 15种检查类型
│   ├── dimension_min/max/range (尺寸限制)
│   ├── text_presence/absence (文本存在/缺失)
│   └── ... (其他类型)
├── 双路径路由
│   ├── text: 纯文本审查
│   ├── visual: 视觉审查
│   └── dual: 双路径 (A级强制升级)
├── 结果标准化
│   └── CheckResult (统一结果格式)
└── 批量执行+统计

加载流程:
1. 查找 definitions_dir/*.yaml
2. yaml.safe_load() 解析
3. CheckpointDefinition.from_yaml() 转换
4. 按专业和类型索引

关键属性:
├── total_count: 总检查点数
├── all_checkpoints: 所有检查点列表
├── _by_discipline: 按专业分组的检查点
└── _by_type: 按类型分组的检查点

方法:
├── resolve_route(cp): 解析检查点路由
├── execute_batch(): 批量执行
└── get_stats(): 获取统计信息
```

## 三、调试关键点
| 问题 | 排查方向 |
|------|----------|
| 模块导入失败 | 检查 PYTHONPATH 是否包含 src/ |
| 检查点加载失败 | 检查 definitions/*.yaml 文件格式 |
| Agent创建失败 | 检查 agent_configs 表配置 |
| LLM调用失败 | 检查环境变量 API Key |
| 图纸处理失败 | 检查 DXF 文件路径和格式 |
| 干跑模式通过但正式运行失败 | 检查 LLM 连接和配额 |
| 报告生成失败 | 检查 output_dir 权限 |

## 四、快速诊断命令
```bash
# 干跑测试 (不调用LLM)
cd src
python -m v7.master_v7 --dry-run

# 检查模块导入
python -c "from v7.checkpoints import CheckpointEngine; print('OK')"
python -c "from v7.llm import LLMFactory; print('OK')"
python -c "from v7.agents import AGENT_REGISTRY; print('OK')"

# 检查检查点
python -c "from v7.checkpoints import CheckpointEngine; e=CheckpointEngine(); print(f'{e.total_count} checkpoints')"

# 检查数据库
python -c "from v7.db import get_db; db=get_db(); print('DB OK')"
```
