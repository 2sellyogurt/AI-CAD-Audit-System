# AI-CAD-Audit-System v7.0 后端技术文档

> 完整覆盖：架构设计、模块职责、数据库Schema、配置系统、全部API端点（含请求/响应格式）

---

## 一、系统架构

```
用户入口
  ├─ launch.bat ──> python -m v7.admin.server  (统一入口)
  │                   ├─ admin/server.py   HTTP服务器 :2708
  │                   │   ├─ do_GET/POST/PUT/DELETE  路由分发
  │                   │   ├─ admin/pages/*.py         6个管理页面
  │                   │   └─ index.html               SPA前端
  │                   └─ master_v7.py      CLI审查入口
  │
  └─ 核心审查流水线
       DrawingExtractor → CheckpointEngine → AgentOrchestrator
       → ProblemPool → RationalityEngine → SQLite + docx报告
```

| 层 | 组件 | 文件 |
|----|------|------|
| 入口 | HTTP服务器 + 路由 | `admin/server.py` |
| 页面 | 6个管理后台模块 | `admin/pages/{drawings,rules,agents,api_config,results,system}.py` |
| 引擎 | 检查点引擎 | `checkpoints/engine.py` (YAML驱动, 15种检查类型) |
| 引擎 | Agent集群调度 | `scheduler/orchestrator.py` (并行+异常恢复) |
| 引擎 | 问题池 | `problem_pool/pool.py` (统一溯源+双路径) |
| 引擎 | 合理性评估 | `rationality_engine.py` (R0-R3四级评分) |
| 数据 | SQLite持久化 | `db/schema.py` (12表, WAL模式) |
| 数据 | 配置加密 | `secure_config.py` (Fernet对称加密) |
| AI | LLM适配器 | `llm/` (5提供商+故障转移) |
| 预处理 | 图纸解析 | `preprocessor/` (ezdxf+CAD打印+分类) |

---

## 二、数据库Schema

**引擎**: SQLite 3, WAL模式, 外键ON  
**文件**: `src/v7/v7_data.db`

### 2.1 核心业务表

**projects** — 项目定义
| 列 | 类型 | 说明 |
|----|------|------|
| id | INTEGER PK | 自增主键 |
| name | TEXT | 项目名称 |
| dxf_dir | TEXT | 图纸目录路径 |
| output_dir | TEXT | 输出目录路径 |
| description | TEXT | 项目描述 |
| created_at | TEXT | 创建时间 |
| updated_at | TEXT | 更新时间 |

**drawings** — 图纸文件
| 列 | 类型 | 说明 |
|----|------|------|
| id | INTEGER PK | 自增主键 |
| project_id | INTEGER FK→projects | 所属项目 |
| filename | TEXT | 文件名 |
| file_path | TEXT | 文件路径 |
| discipline | TEXT | 专业分类 |
| text_content | TEXT | 提取的文本内容 |
| file_size_kb | INTEGER | 文件大小(KB) |
| text_entities | INTEGER | 文本实体数 |
| status | TEXT | pending/scanning/ready/error |
| uploaded_at | TEXT | 上传时间 |

**reviews** — 审查会话
| 列 | 类型 | 说明 |
|----|------|------|
| id | INTEGER PK | 自增主键 |
| project_id | INTEGER FK→projects | 所属项目 |
| mode | TEXT | cached (离线模式) |
| status | TEXT | pending/running/completed |
| total_issues | INTEGER | 问题总数 |
| severity_a/b/c/d | INTEGER | 各级严重度计数 |
| total_conflicts | INTEGER | 空间冲突数 |
| elapsed_ms | INTEGER | 耗时(毫秒) |
| started_at | TEXT | 开始时间 |
| finished_at | TEXT | 完成时间 |
| result_json | TEXT | 结果摘要JSON |

**review_issues** — 问题明细
| 列 | 类型 | 说明 |
|----|------|------|
| id | INTEGER PK | 自增主键 |
| review_id | INTEGER FK→reviews CASCADE | 所属审查 |
| issue_id | TEXT | 问题唯一ID |
| checkpoint_id | TEXT | 对应检查点ID |
| discipline | TEXT | 专业 |
| severity | TEXT | A/B/C/D |
| standard_code | TEXT | 规范编号(如 GB50016) |
| finding | TEXT | 问题描述 |
| fix | TEXT | 修复建议 |
| drawing_name | TEXT | 关联图纸名 |
| location | TEXT | 位置信息 |
| confidence | TEXT | 置信度(high/medium/low) |
| route_used | TEXT | 审查路径(text/visual/dual) |
| rationality | TEXT | 合理性评级(R0-R3) |
| rationality_score | INTEGER | 合理性分数(0-100) |
| status | TEXT | new/confirmed/rejected/fixed |

### 2.2 配置表

**checkpoints** — 检查点规则
| 列 | 说明 |
|----|------|
| id (TEXT PK) | 唯一标识 |
| name | 名称 |
| discipline | 专业 |
| check_type | 检查类型(15种) |
| route | 审查路径(text/visual/dual) |
| severity | 严重度(A/B/C/D) |
| priority | 优先级 |
| standard_code | 规范编号 |
| standard_clause | 规范条款 |
| description | 检查描述 |
| enabled | 启用状态(0/1) |
| config_json | 额外配置JSON |

**agent_configs** — Agent配置
| 列 | 说明 |
|----|------|
| id (TEXT PK) | agent_building, agent_structure 等 |
| name | 显示名称 |
| discipline | 专业 |
| system_prompt | 系统提示词 |
| llm_provider | LLM提供商 |
| llm_model | 模型名称 |
| max_concurrent | 最大并发数 |
| enabled | 启用状态 |

**api_keys** — LLM API密钥(加密)
| 列 | 说明 |
|----|------|
| provider (TEXT PK) | zhipu/deepseek/openai/doubao/ollama |
| api_key | 加密存储的密钥 |
| base_url | API基础URL |
| text_model | 文本模型名 |
| vision_model | 视觉模型名 |
| is_default | 是否默认提供商 |

**config_versions** — 配置变更审计
| 列 | 说明 |
|----|------|
| target_type | 变更目标类型 |
| target_id | 变更目标ID |
| field_name | 变更字段 |
| old_value | 旧值 |
| new_value | 新值 |

**settings** — 系统设置(键值对)

### 2.3 核心函数

| 函数 | 说明 |
|------|------|
| `get_db()` | 线程本地连接 |
| `init_db()` | 建表+插入schema版本 |
| `backup_database(path)` → str | 热备份(使用SQLite backup API) |
| `restore_database(path)` | 从备份恢复 |
| `list_backups()` → list | 备份文件列表 |

---

## 三、配置系统

### 3.1 环境变量 (.env)

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `V7_ADMIN_PORT` | 2708 | 管理后台端口 |
| `V7_REVIEW_PORT` | 8080 | 复核界面端口 |
| `V7_PROJECT_NAME` | 施工图审查项目 | 项目名称 |
| `V7_INPUT_DIR` | input | 图纸输入目录 |
| `V7_OUTPUT_DIR` | output | 输出目录 |
| `V7_CORS_ORIGINS` | localhost:2708,8080 | CORS白名单 |
| `V7_SECRET_KEY` | (必填) | Flask密钥 |
| `ZHIPU_API_KEY` | - | 智谱AI密钥 |
| `DEEPSEEK_API_KEY` | - | DeepSeek密钥 |
| `OPENAI_API_KEY` | - | OpenAI密钥 |
| `DOUBAO_API_KEY` | - | 豆包密钥 |
| `OLLAMA_BASE_URL` | http://localhost:11434/v1 | Ollama地址 |

### 3.2 配置加载器 (config_loader.py)

```python
from v7.config_loader import get_config
cfg = get_config()

cfg.admin_port    # int: 管理后台端口
cfg.review_port   # int: 复核界面端口
cfg.input_dir     # Path: 图纸输入目录
cfg.output_dir    # Path: 输出目录
cfg.cors_origins  # list: CORS允许源
```

自动探测项目根目录（向上找含 `v7/` 的目录），自动加载 `.env` / `.env.template`。

### 3.3 安全配置 (secure_config.py)

API密钥使用 Fernet 对称加密存储，主密钥来源：
1. 环境变量 `AI_REVIEW_MASTER_KEY` (优先)
2. 机器指纹 `COMPUTERNAME + USERNAME` 的 SHA256

```python
from v7.secure_config import (
    get_mode,          # → 'proxy'|'external'
    get_provider,      # → 当前提供商
    get_api_key,       # → 解密后的Key
    set_api_key,       # (provider, key) → 加密存储
    get_all_providers, # → 全部提供商配置(不含密钥)
)
```

### 3.4 审查引擎配置 (config.yaml)

```yaml
llm:
  api_timeout_seconds: 120
  api_max_retries: 2
  global_max_concurrent: 10
  providers:
    doubao: {base_url, text_model, vision_model}
    zhipu:  {base_url, text_model, vision_model}
    deepseek: {base_url, text_model}
    openai: {base_url, text_model, vision_model}
    ollama: {base_url}
  routing:
    default_text_provider: doubao
    default_vision_provider: doubao
    fallback_chain: [doubao, zhipu, deepseek, openai]
review:
  dual_path:          # A/B级强制视觉+文本双路径
  checkpoint_engine:  # max_checkpoints_per_agent: 50
cad:
  supported_versions: [AutoCAD 2018+, 中望CAD, 浩辰CAD]
  print_dpi: 300
  classification_rules:  # 文件名关键词→专业映射
```

---

## 四、认证体系

| 项目 | 说明 |
|------|------|
| 密码算法 | pbkdf2_hmac(sha256, 200000轮) + salt |
| 存储位置 | `src/v7/.admin_password` |
| 会话Token | SHA256("v7_"+密码哈希)[:32] |
| Cookie | `v7_admin_token`; HttpOnly; SameSite=Strict; Max-Age 30天 |
| 登录限流 | 每IP每5分钟最多5次 |
| 首次登录 | 无密码文件时任意密码可登录，引导设置密码 |

---

## 五、审查执行流程

```
POST /api/review/start (或 /admin/api/review/start)
  ↓
后台线程 _run_review():
  1. CheckpointEngine()        加载180+ YAML检查点 (15专业)
  2. DrawingExtractor()        扫描 input/ 下DXF文件
  3. DrawingScanner().scan()   识别图纸涉及的专业
  4. AgentOrchestrator()       创建11个专业Agent + 1个总工Agent
  5. 并行执行                  各Agent对相关图纸执行检查
  6. ProblemPool()             统一收集所有问题(含完整溯源链)
  7. annotate_all_rationality() R0-R3合理性评分
  8. 更新 _review_cache        内存缓存(SAP+管理后台共享)
  9. _persist_review_to_db()   持久化到SQLite
  ↓
启动时: _restore_cache_from_db() 从SQLite恢复最近一次审查结果
```

---

## 六、全部API端点

### 6.1 公共API (无需认证)

| 方法 | 路径 | 请求/参数 | 响应 |
|------|------|-----------|------|
| GET | `/api/health` | - | `{"status":"ok","version":"7.0","port":2708,"ready":bool}` |
| GET | `/api/stats` | - | `{"disciplines":N,"available":N,"pending":0,"totalIssues":N,"bySeverity":{"A","B","C","D"},"byRationality":{"R0","R1","R2","R3"},"highConfConflicts":N,"totalConflicts":N}` |
| GET | `/api/issues` | `?severity=A&rationality=R0&discipline=building&search=关键词` | `{"total":N,"items":[{id,discipline,severity,checkpoint_id,standard_code,finding,fix,drawing_name,location,confidence,route_used,rationality}]}` |
| GET | `/api/issues/{id}` | - | 单条问题完整对象 |
| GET | `/api/conflicts` | - | `{"total":N,"items":[{type,severity,confidence,floor,description,involved}]}` |
| GET | `/api/reports/scene1` | - | 场景1报告.docx (需审查后生成) |
| GET | `/api/reports/scene2` | - | 场景2报告.docx |
| GET | `/api/reports/scene3` | - | 场景3报告.docx |
| GET | `/api/config` | - | `{"mode":"proxy","provider":"","configured_providers":[],"encryption_available":true}` |
| GET | `/api/review/progress` | - | `{"ready":bool,"taskId":"","issues":N,"conflicts":N}` |
| POST | `/api/review/start` | `{}` | `{"taskId":"abc123","status":"started","mode":"cached"}` |

### 6.2 认证API

| 方法 | 路径 | 请求体 | 响应 |
|------|------|--------|------|
| POST | `/admin/api/login` | `{"password":"..."}` | `{"ok":true,"firstLogin":bool}` + Set-Cookie |
| POST | `/admin/api/set-password` | `{"password":"..."}` (≥6位) | `{"ok":true}` + Set-Cookie |

### 6.3 仪表盘 (server.py 内联)

| 方法 | 路径 | 说明 | 响应 |
|------|------|------|------|
| GET | `/admin/dashboard` | 仪表盘页面 | HTML |
| GET | `/admin/api/stats` | 系统统计 | `{"checkpoints":N,"agents":N,"reviews":N,"issues":N,"drawings":N,"projects":N}` |
| POST | `/admin/api/review/start` | 启动审查 | `{"status":"started","taskId":"..."}` |
| GET | `/admin/api/review/progress` | 审查进度 | `{"ready":bool,"issues":N,"conflicts":N}` |
| GET | `/admin/api/issues?severity=A` | 问题列表 | `{"total":N,"items":[...]}` |

### 6.4 图纸管理 (admin/pages/drawings.py)

| 方法 | 路径 | 请求体/参数 | 响应 |
|------|------|------------|------|
| GET | `/admin/drawings` | - | HTML页面 |
| GET | `/admin/api/drawings/projects` | - | `{"projects":[{id,name,dxf_dir,output_dir,drawing_count,pending_count,ready_count,last_review}]}` |
| POST | `/admin/api/drawings/projects` | `{"name":"...","dxf_dir?":"...","output_dir?":"...","description?":"..."}` | `{"ok":true,"id":N,"name":"..."}` |
| DELETE | `/admin/api/drawings/projects/{id}` | - | `{"ok":true}` (级联删除图纸) |
| GET | `/admin/api/drawings/list?project_id=N` | `project_id` (必填) | `{"ok":true,"drawings":[{id,filename,discipline,file_size_kb,text_entities,status}],"project_id":N}` |
| POST | `/admin/api/drawings/upload` | `{"project_id":N,"file_path":"..."}` | `{"ok":true,"id":N,"filename":"...","dest_path":"...","discipline":"...","file_size_kb":N,"text_entities":N}` |
| POST | `/admin/api/drawings/scan` | `{"project_id":N}` | `{"ok":true,"scanned":N}` |
| GET | `/admin/api/drawings/{id}` | - | `{"ok":true,"drawing":{...完整信息含text_content}}` |
| DELETE | `/admin/api/drawings/{id}` | - | `{"ok":true}` |

### 6.5 规则管理 (admin/pages/rules.py)

| 方法 | 路径 | 请求体/参数 | 响应 |
|------|------|------------|------|
| GET | `/admin/rules` | - | HTML页面 |
| GET | `/admin/api/rules/list?discipline=building&search=GB` | 可选过滤 | `{"items":[{id,name,discipline,check_type,route,severity,priority,standard_code,standard_clause,description,enabled}],"total":N}` |
| POST | `/admin/api/rules/reload` | - | `{"ok":true,"count":N}` (从YAML重载) |
| POST | `/admin/api/rules/toggle` | `{"ids":["id1","id2"],"enabled":true}` | `{"ok":true,"count":N}` |
| POST | `/admin/api/rules` | `{"id":"...","name":"...","discipline":"building","check_type":"free_review","route":"text","severity":"B","priority":0,"standard_code":"GB50016","standard_clause?":"...","description?":"...","enabled?":1,"config_json?":{}}` | `{"ok":true,"id":"..."}` |
| GET | `/admin/api/rules/{id}` | - | 检查点完整对象 |
| PUT | `/admin/api/rules/{id}` | 部分更新字段(同create) | `{"ok":true,"id":"..."}` |
| DELETE | `/admin/api/rules/{id}` | - | `{"ok":true}` |

### 6.6 Agent配置 (admin/pages/agents.py)

| 方法 | 路径 | 请求体/参数 | 响应 |
|------|------|------------|------|
| GET | `/admin/agents` | - | HTML页面 |
| GET | `/admin/api/agents/list` | - | `{"agents":[{id,name,discipline,role_title,system_prompt,llm_provider,llm_model,max_concurrent,enabled}]}` |
| GET | `/admin/api/agents/{id}` | - | 单个Agent完整对象 |
| PUT | `/admin/api/agents/{id}` | `{"system_prompt?":"...","llm_provider?":"zhipu","llm_model?":"glm-4","max_concurrent?":3,"enabled?":1}` | 更新后的Agent对象 (变更自动记录config_versions) |
| POST | `/admin/api/agents/{id}/reset` | - | 重置为AGENT_REGISTRY默认Prompt |

> **Agent ID格式**: `agent_{discipline}` (如 `agent_building`, `agent_structure`, `agent_hvac` 等11个)

### 6.7 API密钥配置 (admin/pages/api_config.py)

| 方法 | 路径 | 请求体/参数 | 响应 |
|------|------|------------|------|
| GET | `/admin/api-config` | - | HTML页面 |
| GET | `/admin/api-config/list` | - | `{"ok":true,"providers":[{provider,name,base_url,text_model,vision_model,api_key_masked,has_key,is_default}],"total":N}` |
| PUT | `/admin/api-config/{provider}` | `{"base_url":"...","text_model":"...","vision_model":"...","api_key":"..."}` | `{"ok":true,"provider":"zhipu","masked_key":"abc***xyz"}` |
| POST | `/admin/api-config/{provider}/test` | `{"api_key":"...","base_url":"...","text_model":"..."}` | `{"ok":true,"models":["..."],"elapsed_ms":N}` 或 `{"ok":false,"error":"..."}` |
| PUT | `/admin/api-config/default` | `{"provider":"zhipu"}` | `{"ok":true,"provider":"zhipu"}` |
| GET | `/admin/api-config/costs` | - | `{"ok":true,"by_provider":[],"total_cost":0,"total_calls":0}` |

### 6.8 审查结果 (admin/pages/results.py)

| 方法 | 路径 | 请求体/参数 | 响应 |
|------|------|------------|------|
| GET | `/admin/results` | - | HTML页面 |
| GET | `/admin/api/results/list?severity=A&discipline=building&status=new&search=词` | 可选多条件 | `{"total":N,"items":[{id,review_id,issue_id,discipline,severity,standard_code,finding,fix,drawing_name,location,confidence,route_used,rationality,rationality_score,status}]}` |
| GET | `/admin/api/results/stats` | - | `{"total":N,"aCount":N,"bCount":N,"cCount":N,"dCount":N,"spatialConflicts":N,"disciplines":[],"byStatus":{"new","confirmed","rejected","fixed"}}` |
| GET | `/admin/api/results/reviews` | - | `{"reviews":[{id,status,total_issues,created_at}]}` (最近50条) |
| GET | `/admin/api/results/export?format=csv&severity=A&discipline=building` | 可选过滤 | `text/csv` 文件下载 |
| GET | `/admin/api/results/diff?id1=N&id2=M` | `id1`, `id2` | `{"summary":{"added","removed","changed","total1","total2"},"added":[],"removed":[],"changed":[]}` |
| GET | `/admin/api/results/{id}` | - | 单条问题完整对象 |
| PUT | `/admin/api/results/{id}/status` | `{"status":"confirmed","note?":"已确认"}` | `{"ok":true,"status":"confirmed","note":"..."}` |
| PUT | `/admin/api/results/batch` | `{"ids":["...","..."],"status":"fixed","note?":"批量修复"}` | `{"ok":true,"updated":N}` |

### 6.9 系统维护 (admin/pages/system.py)

| 方法 | 路径 | 请求体/参数 | 响应 |
|------|------|------------|------|
| GET | `/admin/system` | - | HTML页面 |
| GET | `/admin/api/system/status` | - | `{"db":{"path":"...","size":N,"projects":N,"drawings":N,"checkpoints":N,"reviews":N,"issues":N},"directories":{"output":"...","cache":"..."},"system":{"python":"...","platform":"...","pid":N,"child_processes":N}}` |
| GET | `/admin/api/system/caches` | - | `{"caches":[{filename,size,modified,days_old,expired}],"total":N}` |
| DELETE | `/admin/api/system/caches` | - | `{"ok":true,"deleted":N}` |
| DELETE | `/admin/api/system/caches/expired` | - | `{"ok":true,"deleted":N}` (>30天) |
| DELETE | `/admin/api/system/caches/{filename}` | - | `{"ok":true,"filename":"..."}` |
| GET | `/admin/api/system/logs?source=server&lines=200` | `source`: server/admin/review/audit, `lines`: 1-2000 | `{"source":"...","label":"...","lines":N,"content":"...","available_sources":["server","admin","review","audit"]}` |
| POST | `/admin/api/system/reload-checkpoints` | - | `{"ok":true,"count":N}` |
| POST | `/admin/api/system/remigrate` | - | `{"ok":true,"result":{...}}` |
| POST | `/admin/api/system/backup-db` | - | `{"ok":true,"backup_path":"...","backup_name":"...","size":N}` |
| GET | `/admin/api/system/backups` | - | `{"ok":true,"backups":[{filename,path,size,size_formatted,modified}]}` |
| POST | `/admin/api/system/restore-db` | `{"backup_path":"..."}` | `{"ok":true,"message":"恢复成功"}` |
| GET | `/admin/api/system/config-versions` | - | `{"ok":true,"versions":[{id,target_type,target_id,field_name,old_value,new_value,created_at}]}` (最近50条) |

---

## 七、Agent注册表

| ID | Agent类 | 专业 | 审查方式 |
|----|---------|------|----------|
| `building` | BuildingAgent | 建筑 | 文本+视觉 |
| `structure` | StructureAgent | 结构 | 文本+视觉 |
| `hvac` | HvacAgent | 暖通 | 文本+视觉 |
| `plumbing` | PlumbingAgent | 给排水 | 文本+视觉 |
| `electrical` | ElectricalAgent | 电气 | 文本+视觉 |
| `fire` | FireAgent | 消防 | 文本+视觉 |
| `free_review` | FreeReviewAgent | 自由审查 | 文本 |
| `curtain_wall` | CurtainWallAgent | 幕墙 | 文本+视觉 |
| `decoration` | DecorationAgent | 装饰 | 文本+视觉 |
| `landscape` | LandscapeAgent | 景观 | 文本+视觉 |
| `foundation_pit` | FoundationPitAgent | 基坑 | 文本+视觉 |

---

## 八、检查点类型 (15种)

| 类型 | 说明 | 路由 |
|------|------|------|
| `free_review` | 自由审查 | text |
| `dimension_check` | 尺寸检查 | text |
| `clearance_check` | 净距检查 | text |
| `fire_code_check` | 消防规范检查 | text+visual |
| `structural_logic` | 结构逻辑检查 | text |
| `material_check` | 材料检查 | text |
| `label_consistency` | 标注一致性 | text |
| `spatial_conflict` | 空间冲突 | visual |
| `egress_check` | 疏散检查 | dual |
| `accessibility_check` | 无障碍检查 | text |
| `waterproof_check` | 防水检查 | text |
| `ventilation_check` | 通风检查 | text |
| `electrical_load` | 电气负荷 | text |
| `energy_code` | 节能规范 | text |
| `template` | 模板(示例) | text |

---

## 九、合理性评级

| 等级 | 分数 | 含义 |
|------|------|------|
| R0 | 0-30 | 不可行（严重幻觉/与图纸矛盾） |
| R1 | 30-55 | 非最优（部分合理但遗漏关键因素） |
| R2 | 55-80 | 合理（逻辑自洽，符合规范） |
| R3 | 80-100 | 优秀（精确、深度分析、可操作性强） |

---

## 十、LLM故障转移链

```
doubao (默认) → zhipu → deepseek → openai
```

- 单次超时: 120秒
- 最大重试: 每提供商2次
- 全局并发限制: 10
- 每Agent并发限制: 5

---

## 十一、目录结构

```
src/v7/
├── admin/                管理后台
│   ├── server.py         统一HTTP服务器
│   ├── pages/            6个管理页面模块
│   └── static/           CSS
├── agents/               11专业Agent集群
├── checkpoints/          检查点引擎(YAML驱动)
│   └── definitions/      15个专业定义文件
├── config/               项目配置
├── db/                   SQLite持久化层
├── llm/                  LLM适配器(5提供商)
├── preprocessor/         图纸预处理管线(14模块)
├── problem_pool/         统一问题池
├── report_generation/    三场景报告生成
├── report_v7/            角色报告生成
├── review_ui/            人工复核Flask界面
├── scanner/              图纸专业扫描
├── scheduler/            Agent并行调度器
├── audit/                假阴性审计
├── cross_drawing/        跨图纸上下文分析
├── tests/                单元测试
├── index.html            SPA前端
├── launcher.py           桌面启动器(GUI)
├── master_v7.py          CLI主控入口
├── config.yaml           审查引擎配置
├── config_loader.py      统一配置加载
├── secure_config.py      API密钥加密存储
└── requirements.txt      依赖清单
```

---

*文档生成时间: 2026-06-19 | 基于 v7.0 源码 + 54端点LOOP自动化筛查通过*
