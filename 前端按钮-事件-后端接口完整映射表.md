# AI-CAD-Audit-System v7.0 前端按钮-事件-后端接口完整映射表

> 本文档通过逐页逐按钮推演，完整列出所有前端交互元素、点击响应事件及其对应的后端 API 接口。

---

## 一、公共导航与认证（admin/server.py）

| 页面/区域 | 交互元素 | 前端事件函数 | 请求方法 | 后端接口路径 | 请求体/参数 | 后端处理函数 |
|---|---|---|---|---|---|---|
| 登录页 | 登录按钮 | `login()` (内联) | POST | `/admin/api/login` | `{password}` | `do_POST()` 内联处理 |
| 登录页 | 设置密码按钮 | `setPassword()` (内联) | POST | `/admin/api/set-password` | `{password}` | `do_POST()` 内联处理 |
| 顶部导航 | 退出登录链接 | `logout()` (内联) | — | — | — | 纯前端清除 Cookie，无后端 API |
| 仪表盘 | 启动审查按钮 | `startReview()` | POST | `/admin/api/review/start` | — | `do_POST()` → `_run_review()` |
| 仪表盘 | 统计卡片加载 | 页面加载时自动 | GET | `/admin/api/stats` | — | `do_GET()` → DB 查询 |
| 公共 | 导航菜单项 | `<a href="...">` 直接跳转 | GET | `/admin/dashboard`, `/admin/drawings`, `/admin/rules`, `/admin/agents`, `/admin/api-config`, `/admin/results`, `/admin/system` | — | `render_page()` 路由分发 |
| 公共API | 健康检查 | — | GET | `/api/health` | — | `do_GET()` → `_json()` |
| 公共API | 统计数据 | — | GET | `/api/stats` | — | `do_GET()` → `_review_cache` |
| 公共API | 问题列表 | — | GET | `/api/issues` | — | `do_GET()` → `_review_cache` |
| 公共API | 问题详情 | — | GET | `/api/issues/{id}` | — | `do_GET()` → `_review_cache` |
| 公共API | 冲突列表 | — | GET | `/api/conflicts` | — | `do_GET()` → `_review_cache` |
| 公共API | 报告场景1-3 | — | GET | `/api/reports/scene{1-3}` | — | `do_GET()` → `_file()`（需审查后生成 .docx） |
| 公共API | 配置信息 | — | GET | `/api/config` | — | `do_GET()` → `get_all_providers()` |
| 公共API | 审查进度 | — | GET | `/api/review/progress` | — | `do_GET()` → `_review_cache` |
| 已认证 | 审查进度 | — | GET | `/admin/api/review/progress` | — | `do_GET()` → `_review_cache` |
| 已认证 | 问题列表(带筛选) | — | GET | `/admin/api/issues?severity={level}` | — | `do_GET()` → `_review_cache` |
| 公共API | 启动审查(公开) | — | POST | `/api/review/start` | — | `do_POST()` → `_run_review()` |

---

## 二、图纸管理页（drawings.py）

| 交互元素 | 前端事件函数 | 请求方法 | 后端接口路径 | 请求体/参数 | 后端处理函数 |
|---|---|---|---|---|---|
| + 新建项目按钮 | `openNewProjectModal()` → `createProject()` | POST | `/admin/api/drawings/projects` | `{name, dxf_dir?, output_dir?, description?}` | `_handle_create_project(body)` |
| ↑ 上传图纸按钮 | `openUploadModal()` → `uploadDrawing()` | POST | `/admin/api/drawings/upload` | `{project_id, file_path}` | `_handle_upload_drawing(body)` |
| ▶ 启动审查按钮 | `startReviewFromPage()` | POST | `/admin/api/review/start` | — | 复用 `do_POST()` 接口 |
| 项目行-查看按钮 | `toggleProject(projectId)` | GET | `/admin/api/drawings/list?project_id={id}` | — | `_handle_get_drawings(pid)` |
| 项目行-删除按钮 | `confirmDeleteProject(id, name)` → 确认后 | DELETE | `/admin/api/drawings/projects/{id}` | — | `_handle_delete_project(pid)` |
| 图纸行-预览按钮 | `previewDrawing(drawingId)` | GET | `/admin/api/drawings/{id}` | — | `_handle_get_drawing_detail(did)` |
| 图纸行-删除按钮 | `confirmDeleteDrawing(id, filename, projectId)` → 确认后 | DELETE | `/admin/api/drawings/{id}` | — | `_handle_delete_drawing(did)` |
| 🔍 扫描分类图纸专业 | `scanProjectDrawings(projectId)` | POST | `/admin/api/drawings/scan` | `{project_id}` | `_handle_scan_drawings(body)` |
| 项目行-展开/收起箭头 | `toggleProject(projectId)` | GET | `/admin/api/drawings/list?project_id={id}` | — | `_handle_get_drawings(pid)` |
| 拖拽上传区点击 | 聚焦文件路径输入框 | — | — | — | 纯前端操作 |
| 模态框-点击外部关闭 | click 事件监听 | — | — | — | 纯前端操作 |
| 模态框-ESC键关闭 | keydown 事件监听 | — | — | — | 纯前端操作 |
| 模态框-取消/关闭按钮 | 关闭模态框 | — | — | — | 纯前端操作 |
| 模态框-创建项目按钮 | `createProject()` | POST | `/admin/api/drawings/projects` | 同上 | 同上 |
| 模态框-上传按钮 | `uploadDrawing()` | POST | `/admin/api/drawings/upload` | 同上 | 同上 |
| 页面加载 | 自动加载项目列表 | GET | `/admin/api/drawings/projects` | — | `_handle_get_projects()` |

---

## 三、Agent 配置页（agents.py）

| 交互元素 | 前端事件函数 | 请求方法 | 后端接口路径 | 请求体/参数 | 后端处理函数 |
|---|---|---|---|---|---|
| Agent 卡片点击 | `selectAgent(a.id)` | GET | `/admin/api/agents/{id}` | — | `_get_agent(agent_id)` |
| Agent 卡片开关 | `toggleAgent(agentId, checked)` | PUT | `/admin/api/agents/{id}` | `{enabled: 1/0}` | `_update_agent(agent_id, data)` |
| 保存按钮 | `saveAgent()` | PUT | `/admin/api/agents/{id}` | `{system_prompt, llm_provider, llm_model, max_concurrent}` | `_update_agent(agent_id, data)` |
| 重置为默认按钮 | `confirmReset()` → 双击 → `doReset()` | POST | `/admin/api/agents/{id}/reset` | — | `_reset_agent(agent_id)` |
| Prompt 文本域输入 | input 事件监听 | — | — | — | 纯前端实时更新字符计数 |
| 页面加载 | 自动加载 Agent 列表 | GET | `/admin/api/agents/list` | — | `_get_all_agents()` |

---

## 四、规则与检查点页（rules.py）

| 交互元素 | 前端事件函数 | 请求方法 | 后端接口路径 | 请求体/参数 | 后端处理函数 |
|---|---|---|---|---|---|
| 🔄 刷新 YAML 按钮 | `reloadYaml()` | POST | `/admin/api/rules/reload` | — | `_api_reload()` |
| + 新建检查点按钮 | `openCreateModal()` → `saveCheckpoint()` | POST | `/admin/api/rules` | `{id, name, discipline, check_type, route, severity, standard_code?, standard_clause?, description?, priority?, enabled?, config_json?}` | `_api_create()` |
| 全选复选框 | `toggleSelectAll()` | — | — | — | 纯前端操作 |
| 单行复选框 | `toggleItem()` | — | — | — | 纯前端操作 |
| 批量启用按钮 | `batchToggle(true)` | POST | `/admin/api/rules/toggle` | `{ids: [...], enabled: true}` | `_api_toggle()` |
| 批量禁用按钮 | `batchToggle(false)` | POST | `/admin/api/rules/toggle` | `{ids: [...], enabled: false}` | `_api_toggle()` |
| 批量删除按钮 | `batchDelete()` → 确认后 | DELETE | `/admin/api/rules/{id}` (循环) | — | `_api_delete()` |
| 单行开关 | `toggleOne(id, enabled)` | POST | `/admin/api/rules/toggle` | `{ids: [id], enabled}` | `_api_toggle()` |
| 编辑按钮 | `openEditModal(id)` → 保存 | GET + PUT | `/admin/api/rules/{id}` (GET) → `/admin/api/rules/{id}` (PUT) | GET: — / PUT: 同新建字段(部分更新) | `_api_detail()` → `_api_update()` |
| 删除按钮 | `deleteOne(id)` → 确认后 | DELETE | `/admin/api/rules/{id}` | — | `_api_delete()` |
| 侧边栏专业项 | `selectDisc(disc)` | GET | `/admin/api/rules/list?discipline={disc}` | — | `_api_list()` |
| 搜索输入框 | `onSearch()` (防抖) | GET | `/admin/api/rules/list?discipline={disc}&search={query}` | — | `_api_list()` |
| 模态框-取消/关闭 | 关闭模态框 | — | — | — | 纯前端操作 |
| 模态框-保存 | `saveCheckpoint()` | POST/PUT | `/admin/api/rules` 或 `/admin/api/rules/{id}` | 同上 | `_api_create()` / `_api_update()` |

---

## 五、审查结果页（results.py）

| 交互元素 | 前端事件函数 | 请求方法 | 后端接口路径 | 请求体/参数 | 后端处理函数 |
|---|---|---|---|---|---|
| 严重级别筛选 | `applyFilters()` | GET | `/admin/api/results/list?severity={level}` | — | `_api_list()` |
| 专业筛选 | `applyFilters()` | GET | `/admin/api/results/list?discipline={disc}` | — | `_api_list()` |
| 状态筛选 | `applyFilters()` | GET | `/admin/api/results/list?status={status}` | — | `_api_list()` |
| 搜索输入框 | `debounceSearch()` | GET | `/admin/api/results/list?search={query}` | — | `_api_list()` |
| 导出 CSV 按钮 | `exportCSV()` | GET | `/admin/api/results/export?format=csv&severity=&discipline=&status=&search=` | — | `_api_export()` |
| 对比模式按钮 | `toggleDiffMode()` → `loadReviewList()` | GET | `/admin/api/results/reviews` | — | `_api_reviews()` |
| 开始对比按钮 | `loadDiff()` | GET | `/admin/api/results/diff?id1={id1}&id2={id2}` | — | `_api_diff()` |
| 关闭对比按钮 | `toggleDiffMode()` | — | — | — | 纯前端操作 |
| 表格整行点击 | `toggleDetail(event, idx)` | — | — | — | 纯前端展开/收起详情 |
| 详情按钮 | `openDetail(idx)` | — | — | — | 纯前端展开详情 |
| 备注文本域输入 | input 事件 | — | — | — | 纯前端输入，随状态更新提交 |
| 确认按钮 | `updateStatus(id, 'confirmed')` | PUT | `/admin/api/results/{id}/status` | `{status: 'confirmed', note?}` | `_api_update_status()` |
| 驳回按钮 | `updateStatus(id, 'rejected')` | PUT | `/admin/api/results/{id}/status` | `{status: 'rejected', note?}` | `_api_update_status()` |
| 已修复按钮 | `updateStatus(id, 'fixed')` | PUT | `/admin/api/results/{id}/status` | `{status: 'fixed', note?}` | `_api_update_status()` |
| 页面加载 | 自动加载结果列表 | GET | `/admin/api/results/list` | — | `_api_list()` |
| 页面加载 | 自动加载统计 | GET | `/admin/api/results/stats` | — | `_api_stats()` |
| 批量状态更新 | (列表页批量操作) | PUT | `/admin/api/results/batch` | `{ids: [...], status}` | `_api_batch_status()` |
| 问题详情获取 | — | GET | `/admin/api/results/{id}` | — | `_api_issue_detail()` |

---

## 六、API 配置页（api_config.py）

| 交互元素 | 前端事件函数 | 请求方法 | 后端接口路径 | 请求体/参数 | 后端处理函数 |
|---|---|---|---|---|---|
| 保存按钮 (每个提供商卡片) | `saveConfig(provider)` | PUT | `/admin/api-config/{provider}` | `{base_url, text_model, vision_model, api_key}` | `_save_provider_to_db(provider, data)` |
| 测试连接按钮 | `testConnection(provider)` | POST | `/admin/api-config/{provider}/test` | `{api_key, base_url, text_model}` | `_test_connection(provider, api_key, base_url, text_model)` |
| 设为默认提供商单选框 | `setDefault(provider)` | PUT | `/admin/api-config/default` | `{provider}` | `_set_default_provider(provider)` |
| 页面加载 | 自动加载提供商列表 | GET | `/admin/api-config/list` | — | `_load_providers_from_db()` |
| 页面加载 | 自动加载费用统计 | GET | `/admin/api-config/costs` | — | `_get_cost_stats()` |
| API Key 输入框 | focus/blur 事件 | — | — | — | 纯前端切换 password/text 类型 |

---

## 七、系统维护页（system.py）

| 交互元素 | 前端事件函数 | 请求方法 | 后端接口路径 | 请求体/参数 | 后端处理函数 |
|---|---|---|---|---|---|
| 🔄 刷新状态按钮 | `sysRefreshStatus()` | GET | `/admin/api/system/status` | — | `_api_status()` |
| 🔄 刷新缓存按钮 | `sysRefreshCaches()` | GET | `/admin/api/system/caches` | — | `_api_list_caches()` |
| ⏳ 清理过期缓存按钮 | `sysClearExpiredCaches()` → 确认后 | DELETE | `/admin/api/system/caches/expired` | — | `_api_clear_expired_caches()` |
| 🗑️ 清空所有缓存按钮 | `sysClearAllCaches()` → 确认后 | DELETE | `/admin/api/system/caches` | — | `_api_clear_caches()` |
| 缓存行-删除按钮 | `sysDeleteCache(fn)` | DELETE | `/admin/api/system/caches/{filename}` | — | `_api_delete_cache(filename)` |
| 日志源下拉框 | `sysRefreshLog()` | GET | `/admin/api/system/logs?source={src}&lines=200` | — | `_api_get_logs(qs)` |
| 搜索过滤输入框 | `sysApplyLogFilter()` | — | — | — | 纯前端过滤 |
| 🔄 刷新日志按钮 | `sysRefreshLog()` | GET | `/admin/api/system/logs?source={src}&lines=200` | — | `_api_get_logs(qs)` |
| 自动刷新开关 | `sysToggleAutoRefresh(checked)` | — | — | — | 纯前端定时器 (10秒) |
| 📥 重新加载检查点按钮 | `sysReloadCheckpoints()` | POST | `/admin/api/system/reload-checkpoints` | — | `_api_reload_checkpoints()` |
| 🔄 重新迁移数据库按钮 | `sysRemigrate()` → 确认后 | POST | `/admin/api/system/remigrate` | — | `_api_remigrate()` |
| 💾 数据库备份按钮 | `sysBackupDb()` | POST | `/admin/api/system/backup-db` | — | `_api_backup_db()` |
| 刷新版本历史按钮 | `sysRefreshVersions()` | GET | `/admin/api/system/config-versions` | — | `_api_config_versions()` |
| 页面加载 | 自动加载系统状态 | GET | `/admin/api/system/status` | — | `_api_status()` |
| 页面加载 | 自动加载缓存列表 | GET | `/admin/api/system/caches` | — | `_api_list_caches()` |
| 页面加载 | 自动加载日志 | GET | `/admin/api/system/logs?source=server&lines=200` | — | `_api_get_logs(qs)` |
| 页面加载 | 自动加载版本历史 | GET | `/admin/api/system/config-versions` | — | `_api_config_versions()` |
| 备份列表获取 | — | GET | `/admin/api/system/backups` | — | `_api_list_backups()` |
| 数据库恢复 | — | POST | `/admin/api/system/restore-db` | `{backup_path}` | `_api_restore_db(body)` |

---

## 八、接口汇总统计

### 按页面统计接口数量

| 页面 | 前端按钮/交互元素数 | 后端 API 接口数 |
|---|---|---|
| 公共导航与认证 (admin/server.py) | 15 | 15 |
| 图纸管理 (drawings.py) | 15 | 8 |
| Agent 配置 (agents.py) | 6 | 4 |
| 规则与检查点 (rules.py) | 14 | 7 |
| 审查结果 (results.py) | 18 | 9 |
| API 配置 (api_config.py) | 6 | 5 |
| 系统维护 (system.py) | 16 | 12 |
| **合计** | **90** | **60** |

### 按 HTTP 方法统计

| 方法 | 接口数量 |
|---|---|
| GET | 30 |
| POST | 16 |
| PUT | 10 |
| DELETE | 4 |

### 关键数据流说明

1. **审查启动流程**: 仪表盘/图纸页 `startReview()` → `POST /admin/api/review/start` → `do_POST()` 写入 `_review_cache` → 后台异步执行 `_run_review()`
2. **图纸上传流程**: 图纸页 `uploadDrawing()` → `POST /admin/api/drawings/upload` → 保存到数据库 + 可选写入磁盘 → 返回图纸ID
3. **状态变更流程**: 结果页 `updateStatus()` → `PUT /admin/api/results/{id}/status` → 更新 `review_issues` 表状态字段
4. **配置保存流程**: API配置页 `saveConfig()` → `PUT /admin/api-config/{provider}` → 写入 `api_keys` 表 + 记录 `config_versions` 版本历史
5. **缓存清理流程**: 系统页 `sysClearAllCaches()` → `DELETE /admin/api/system/caches` → 遍历删除 `.dxf_cache` 目录下所有文件

---

### 校验修正记录

**第三轮（54端点LOOP自动化筛查）**:
- 删除4个不存在端点: `/api/config/building-types`, `disciplines`, `report-scenes`, `conflict-types`
- 修正所有 `server.py` 引用 → `admin/server.py`（旧独立server.py已删除）
- 修正 API配置路径: `/admin/api/api-config/*` → `/admin/api-config/*`
- 修正 Agent ID: `{id}` 实际为 `agent_{discipline}` 格式（如 `agent_building`）
- 修正 `ct == "application/json"` → `ct.startswith("application/json")`（带charset时崩溃）
- 修正 `do_PUT`/`do_DELETE` 路由 `api_config` 前缀特殊处理

**第二轮（逐行源码核对）**:

| 序号 | 问题类型 | 位置 | 修正内容 |
|---|---|---|---|
| 1 | 参数描述错误 | server.py 登录 | 请求体从 `{username, password}` 修正为 `{password}`（无 username 字段） |
| 2 | 参数描述错误 | server.py 设置密码 | 请求体从 `{old_password, new_password}` 修正为 `{password}` |
| 3 | 幻觉（不存在） | server.py 退出登录 | 删除不存在的 `POST /admin/api/logout` 接口，修正为"纯前端清除 Cookie" |
| 4 | 遗漏 | server.py 已认证端点 | 补充 `GET /admin/api/review/progress` 和 `GET /admin/api/issues?severity=` |
| 5 | 参数描述错误 | drawings.py 新建项目 | 请求体从 `{name, description?, building_type?}` 修正为 `{name, dxf_dir?, output_dir?, description?}` |
| 6 | 参数描述严重错误 | drawings.py 上传图纸 | 请求体从 `{project_id, file_name, file_content_base64}` 修正为 `{project_id, file_path}`（通过路径上传，非 base64） |
| 7 | 参数描述不完整 | results.py 状态更新 | 请求体补充 `note?` 可选字段 |
| 8 | 遗漏 | drawings.py | 补充项目行展开/收起箭头点击 |
| 9 | 遗漏 | drawings.py | 补充拖拽上传区点击、模态框外部点击关闭、ESC键关闭 |
| 10 | 遗漏 | agents.py | 补充 Prompt 文本域 input 事件（实时字符计数） |
| 11 | 遗漏 | results.py | 补充表格整行点击展开/收起、备注文本域输入 |

---

*文档生成时间: 基于 AI-CAD-Audit-System v7.0 源码逐行核对后修正（第三轮：54端点LOOP自动化筛查通过）*
